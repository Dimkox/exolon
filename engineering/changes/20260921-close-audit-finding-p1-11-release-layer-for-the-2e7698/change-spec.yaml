{
  "schema_version": 2,
  "change_id": "20260921-close-audit-finding-p1-11-release-layer-for-the-2e7698",
  "objective": {
    "id": "OBJ-001",
    "statement": "Bundle version identity of the Exolon macOS target has a single source of truth (build settings, not Info.plist literals), the project exposes one shared Archive-capable Xcode scheme, the macOS probe no longer asserts the pre-fix gaps, and a committed self-checking Linux verifier (48 binary ACs, 29 mandatory-flipping controls) holds the release-layer claim; the signing clause of audit finding P1-11 stays open with a recorded reason.",
    "success_metric": "release_layer_check.py rc=0 with 0 red AC and 29/29 controls OK on the merged head, plus grok_verify --mode pr PASS on the same fingerprint",
    "target": "rc=0 / red_ac=0 / bad_controls=0"
  },
  "risk": {
    "tier": "red",
    "domains": [
      "frontend"
    ]
  },
  "acceptance_criteria": [
    {
      "id": "AC-001",
      "statement": "Info.plist carries CFBundleShortVersionString as $(MARKETING_VERSION) and CFBundleVersion as $(CURRENT_PROJECT_VERSION) byte-exactly, neither value contains a literal digit, and the remaining nine plist keys are byte-identical to the audited state (digest-pinned), so the bundle version has exactly one source of truth.",
      "evidence": [
        { "test": "engineering/changes/20260921-close-audit-finding-p1-11-release-layer-for-the-2e7698/evidence/release_layer_check.py::plist_version_keys_substituted" },
        { "receipt": "verification" }
      ]
    },
    {
      "id": "AC-002",
      "statement": "Both placeholders resolve to settings that live in the two target-level XCBuildConfiguration blocks (MARKETING_VERSION and CURRENT_PROJECT_VERSION each present exactly once in the Debug and Release target bodies and zero times outside them), which a whole-file grep cannot certify.",
      "evidence": [
        { "test": "engineering/changes/20260921-close-audit-finding-p1-11-release-layer-for-the-2e7698/evidence/release_layer_check.py::marketing_version_in_target_configs" },
        { "receipt": "verification" }
      ]
    },
    {
      "id": "AC-003",
      "statement": "Exactly one .xcscheme exists tree-wide, it is the shared file Exolon.xcodeproj/xcshareddata/xcschemes/Exolon.xcscheme, and no scheme lives under xcuserdata.",
      "evidence": [
        { "test": "engineering/changes/20260921-close-audit-finding-p1-11-release-layer-for-the-2e7698/evidence/release_layer_check.py::shared_scheme_path_exact" },
        { "receipt": "verification" }
      ]
    },
    {
      "id": "AC-004",
      "statement": "The shared scheme is canonical Xcode shape and is genuinely Archive-capable: it parses, has BuildAction/TestAction/LaunchAction/ArchiveAction, its BuildActionEntry sets all five buildFor* flags to YES including buildForArchiving, ArchiveAction buildConfiguration is Release and LaunchAction buildConfiguration is Debug, and every BuildableReference resolves to the single real native target (blueprint 500000000000000000000001, Exolon / Exolon.app, lowercase container: prefix).",
      "evidence": [
        { "test": "engineering/changes/20260921-close-audit-finding-p1-11-release-layer-for-the-2e7698/evidence/release_layer_check.py::scheme_build_for_archiving_yes" },
        { "receipt": "verification" },
        { "receipt": "release_review" }
      ]
    },
    {
      "id": "AC-005",
      "statement": "No test target or dangling test reference is smuggled in: the project still has one native target and zero test targets, and the scheme's TestAction carries zero TestableReference elements.",
      "evidence": [
        { "test": "engineering/changes/20260921-close-audit-finding-p1-11-release-layer-for-the-2e7698/evidence/release_layer_check.py::test_target_count" },
        { "receipt": "verification" },
        { "receipt": "security_review" }
      ]
    },
    {
      "id": "AC-006",
      "statement": "engineering/runbooks/macos-probe.sh no longer asserts the pre-fix gaps as expectations (zero stale EXPECTED lines) and gained the archive step plus the shared-vs-autogenerated scheme discriminator and a codesign flags reader that can actually observe the hardened runtime.",
      "evidence": [
        { "test": "engineering/runbooks/macos-probe.sh::section-A" },
        { "test": "engineering/changes/20260921-close-audit-finding-p1-11-release-layer-for-the-2e7698/evidence/release_layer_check.py::runbook_stale_gap_claims" },
        { "receipt": "verification" }
      ]
    },
    {
      "id": "AC-007",
      "statement": "ENABLE_HARDENED_RUNTIME stays absent from every build configuration and that deferral is recorded as a decision with a reason and a next step in this package (release.md, requirements.md, change-spec), not silently skipped; the verifier turns red if the deferral record disappears while the key is still absent.",
      "evidence": [
        { "test": "engineering/changes/20260921-close-audit-finding-p1-11-release-layer-for-the-2e7698/evidence/release_layer_check.py::hardened_deferral_recorded" },
        { "receipt": "security_review" },
        { "receipt": "release_review" }
      ]
    },
    {
      "id": "AC-008",
      "statement": "The v3_measurements.py cutover is owned: its pinned shared_xcschemes expectation stays at 0 (dated evidence is never edited retroactively), so that tool is expected to turn red on the post-closure tree, and this package records that deleting the scheme to make it green again is the forbidden repair.",
      "evidence": [
        { "test": "engineering/changes/20260921-close-audit-finding-p1-11-release-layer-for-the-2e7698/evidence/release_layer_check.py::v3_shared_scheme_cutover_owned" },
        { "receipt": "verification" },
        { "receipt": "release_review" }
      ]
    },
    {
      "id": "AC-009",
      "statement": "Dated evidence is append-only: the two dated audit reports and the audit measurement tool keep their pinned blobs (worktree == HEAD == pin commit), and no product or evidence file that records the 2026-09-20 audit state was rewritten.",
      "evidence": [
        { "test": "engineering/changes/20260921-close-audit-finding-p1-11-release-layer-for-the-2e7698/evidence/release_layer_check.py::reports_blob_mismatches" },
        { "receipt": "verification" }
      ]
    },
    {
      "id": "AC-010",
      "statement": "The Linux verdict never certifies macOS-only facts: expanded bundle values, scheme-loader acceptance, archive outcome and codesign runtime flags are asserted only by the macOS runbook and handout v2, and the verifier contains no acceptance criterion that pretends to measure them here.",
      "evidence": [
        { "test": "engineering/changes/20260921-close-audit-finding-p1-11-release-layer-for-the-2e7698/evidence/perfile/macos-validation-handout-v2.md::M-01" },
        { "test": "engineering/runbooks/macos-probe.sh::section-C" },
        { "receipt": "release_review" }
      ]
    }
  ],
  "invariants": [
    {
      "id": "INV-001",
      "statement": "The Debug and Release target XCBuildConfiguration blocks stay symmetric: their buildSettings are identical, and ENABLE_HARDENED_RUNTIME is either absent from both or present in both, never one-sided.",
      "evidence": [
        { "test": "engineering/changes/20260921-close-audit-finding-p1-11-release-layer-for-the-2e7698/evidence/release_layer_check.py::target_cfg_symmetric" }
      ]
    },
    {
      "id": "INV-002",
      "statement": "One and only one PBXNativeTarget exists in the project and the shared scheme addresses it; the product type stays com.apple.product-type.application and the bundle identifier com.exolon.remake is untouched.",
      "evidence": [
        { "test": "engineering/changes/20260921-close-audit-finding-p1-11-release-layer-for-the-2e7698/evidence/release_layer_check.py::native_target_count" }
      ]
    },
    {
      "id": "INV-003",
      "statement": "No unresolved $(...) placeholder may ship: every placeholder in Info.plist belongs to the settings actually defined on the target, and the built bundle must contain zero of them (macOS-observed).",
      "evidence": [
        { "test": "engineering/runbooks/macos-probe.sh::section-C" }
      ]
    },
    {
      "id": "INV-004",
      "statement": "A green release-layer verdict must be causal: every part of the plan (plist substitution, scheme, runbook retarget, package records) has an in-memory revert that independently reddens its own acceptance criteria.",
      "evidence": [
        { "test": "engineering/changes/20260921-close-audit-finding-p1-11-release-layer-for-the-2e7698/evidence/release_layer_check.py::undo_fix_is_red" }
      ]
    }
  ],
  "forbidden_outcomes": [
    {
      "id": "FORBID-001",
      "statement": "This change must not decide the marketing version value: editing both sides to agree on a newly chosen number re-creates the dual-source defect and makes the verifier structurally unable to detect a future regression.",
      "evidence": [
        { "test": "engineering/changes/20260921-close-audit-finding-p1-11-release-layer-for-the-2e7698/evidence/release_layer_check.py::marketing_version_value" }
      ]
    },
    {
      "id": "FORBID-002",
      "statement": "ENABLE_HARDENED_RUNTIME must not be enabled in this change, in either configuration, before the first successful build (M-01) is proven on macOS; a one-sided insertion is additionally blocked by INV-001.",
      "evidence": [
        { "test": "engineering/changes/20260921-close-audit-finding-p1-11-release-layer-for-the-2e7698/evidence/release_layer_check.py::hardened_runtime_key_count" }
      ]
    },
    {
      "id": "FORBID-003",
      "statement": "No test target, no TestableReference, no signing identity, no DEVELOPMENT_TEAM value and no .entitlements file may enter the tree through this change.",
      "evidence": [
        { "test": "engineering/changes/20260921-close-audit-finding-p1-11-release-layer-for-the-2e7698/evidence/release_layer_check.py::scheme_dangling_test_refs" }
      ]
    },
    {
      "id": "FORBID-004",
      "statement": "Dated audit evidence must not be rewritten retroactively, and deleting the shared scheme to make v3_measurements.py green again is the explicitly forbidden repair of the recorded cutover.",
      "evidence": [
        { "test": "engineering/changes/20260921-close-audit-finding-p1-11-release-layer-for-the-2e7698/evidence/release_layer_check.py::shared_scheme_count" }
      ]
    },
    {
      "id": "FORBID-005",
      "statement": "No Swift source, no game logic, no pbxproj object-graph surgery (objectVersion/compatibilityVersion/project upgrade) and no xcuserdata commit is allowed in this change; rollback must stay a single git revert.",
      "evidence": [
        { "test": "engineering/changes/20260921-close-audit-finding-p1-11-release-layer-for-the-2e7698/evidence/release_layer_check.py::user_scheme_count" }
      ]
    },
    {
      "id": "FORBID-006",
      "statement": "P1-11 must not be declared fully closed: only the identity and scheme clauses are closed, the signing clause remains open.",
      "evidence": [
        { "test": "engineering/changes/20260921-close-audit-finding-p1-11-release-layer-for-the-2e7698/requirements.md" }
      ]
    }
  ],
  "contracts": {
    "openapi": [],
    "json_schema": [],
    "events": []
  },
  "observability": [
    {
      "id": "SIG-001",
      "metric": "release_layer_check.py exit code and red_ac/bad_controls counters",
      "proves": ["OBJ-001"]
    },
    {
      "id": "SIG-002",
      "metric": "v3_measurements.py exit code 1 on shared_xcschemes after merge (expected cutover signal, not regression)",
      "proves": ["OBJ-001"]
    },
    {
      "id": "SIG-003",
      "metric": "macOS probe lines version_single_source, build_version_single_source, plist_unexpanded_placeholders, showdestinations_rc, archive_rc, codesign_flags",
      "proves": ["OBJ-001"]
    }
  ],
  "rollback": {
    "strategy": "forward_fix",
    "maximum_steps": 1
  },
  "approvals": {
    "required_scopes": [
      "scope_and_design_approval",
      "defer_hardened_runtime",
      "macos_validation_run_by_owner",
      "audit_backlog_partial_closure_note"
    ]
  }
}
