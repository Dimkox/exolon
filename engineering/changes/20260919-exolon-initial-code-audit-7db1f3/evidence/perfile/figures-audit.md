# Аудит цифр двух отчётов (per-file figures audit)

**Объект:** не игра, а сами отчёты —
`engineering/reports/exolon-full-audit-20260920.md` (авторитет, «FA») и
`engineering/reports/exolon-initial-audit.md` + `engineering/reports/exolon-initial-audit-backlog.md` (v1, с корректирующими шапками).

**База замера:** HEAD `52795d13d879ad477a3023cae69978436ff82d1a` (`codex/factory-updated-20260920`),
рабочее дерево `<repo>`. Отчёт FA написан и закоммичен про HEAD `b8aee42`
(`bcacf30` — «docs: full repository audit at b8aee42»); часть заявок FA описывают состояние дерева `b8aee42`
и потому на `52795d1` устарели — такие строки помечены явно.

**Соблюдение read-only:** `git status --porcelain` после всех прогонов содержит только untracked-записи
(`.qwen/`, `__pycache__`, `.grok-stack/runtime/*`, два чужих review-файла другой полосы и мой `perfile/`);
`git diff --stat` пуст; `.git/index` не изменился (`md5sum` до и после = `6a3509fa6381ae975490b1df811820dd`).
Скрипты и логи: `/tmp/perfile-figures/` (`tmx_probe.py`, `cabin_probe.py`, `fam_patched.py`, `lsa.py`,
`exolon-factory-exit-mine.log`, `unknown-flag.log`, `no-postgres-run.log`). Коммитнутые инструменты
(`fullaudit_measurements.py`, `linux_static_audit.py`) копировались в `/tmp` с переназначением
`REPO`/`OUT_DIR`, чтобы ни один байт репозитория не был перезаписан.

**Сокращения в таблице:** `FA` = `engineering/reports/exolon-full-audit-20260920.md`,
`v1` = `engineering/reports/exolon-initial-audit.md`, `v1-bl` = бэклог,
`M` = `engineering/changes/20260919-exolon-initial-code-audit-7db1f3/evidence/fullaudit_measurements.py`,
`B01J` = `…/evidence/fullaudit-b01-spawn.json`, `LSA` = `…/evidence/linux_static_audit.py`,
`DR` = `…/evidence/analysis-docs_researcher-fullaudit.md`, `AR` = `…/evidence/analysis-architect-fullaudit.md`.

| Заявка (§, строка, цифра) | Инструмент в репо | Мой прогон (команда) | Моё число | Вердикт | Контроль перевернулся? |
| --- | --- | --- | --- | --- | --- |
| FA §1.3:14 — **125/125 TMX well-formed** (xmllint) | нет (только утверждение) | `for f in Exolon/Resources/*.tmx; do xmllint --noout "$f" \|\| echo FAIL; done` | 125 файлов, 0 падений | **REPRODUCED** | да: усечённый `L01S10.tmx` → `xmllint exit=1` (parser error line 36) |
| FA §2:26 — xmllint **независим** от python-разбора первички | LSA (`ET.parse`) | `python3 /tmp/perfile-figures/lsa.py` (копия LSA) | `tmx_on_disk=125`, `xml_error`=0, артефакт `linux-static-audit.json` побайтово совпал с коммитнутым | **REPRODUCED** | да: два разных парсера (libxml2 vs xml.etree) дают 125/125 независимо |
| FA §1.3:14 / §2:27 — **153 PNG** целостны (pngcheck) | нет | `pngcheck Exolon/Resources/*.png \| grep -c "^OK"`; `ls Exolon/Resources/*.png \| wc -l` | 153 / 153 OK, exit 0 | **REPRODUCED** | да: побитый байт в IDAT → `invalid IDAT row-filter type (83)`, `pngcheck exit=2` |
| FA §2:32 — «каждый валидатор имел заведомо падающий кейс» | нет | 3 контроля выше + `swiftc -frontend -parse broken.swift` | 3 из 3controls упали как должно | **REPRODUCED** | да (3/3) |
| FA §1.2:13 — **18/18 Swift** через `swiftc -frontend -parse` | нет | `for f in $(find Exolon -name '*.swift'); do swiftc -frontend -parse $f; done` | PASS=18 FAIL=0 (`find Exolon -name '*.swift' \| wc -l` = 18) | **REPRODUCED** | да: битый Swift → `error: expected initial value after '='`, exit=1 |
| FA §1.2:13 / §2:30 — версия тулчейна «**Swift 6.4.0**» | нет | `swiftc --version` | `Swift version 6.4 (swift-6.4-RELEASE)` — «6.4.0» в дереве/в системе не печатается | **NOT REPRODUCIBLE** (метка; 6.4 ≠ 6.4.0) | да: строка версии отличается от заявленной |
| FA §1.2:13 — тип-чек/сборка невозможны без Apple SDK | нет | `swiftc -frontend -typecheck Exolon/GameCore/GameConstants.swift` | `error: no such module 'CoreGraphics'` | **REPRODUCED** | да: `-parse` проходит, `-typecheck` падает → граница потолок/не-потолок различима |
| FA §2:28 — «снятие **9** ModuleNotFoundError-ошибок прогона 19.09» | AR/repo_explorer:300 говорит «**3**», v1:48 — «3 ERROR» | `grep -rn "ModuleNotFound\|No module named" engineering/` | единственное число в артефактах — **3**; «9» не встречается нигде | **NO ARTIFACT** (и противоречит артефакту: 3) | да: другой счётчик в том же evidence-файле |
| FA §3:40 — «все **27** имён `SKTexture(imageNamed:)` резолвятся» | DR:381 (`/tmp/docsaudit/imgnames.py`, не коммитился) | свой regex по `imageNamed: / image: / imageName =` (литералы) vs узкий `SKTexture(imageNamed: "…")` | широкий метрика = **27 уникальных имени, 0 нерезолвленных** ✓; узкий (как написано в FA) = **17** литералов, 23 вызова | **REPRODUCED** по числу 27 (но единица в FA названа неверно: это не только `SKTexture(imageNamed:)`) | да: узкая метрика даёт 17 ≠ 27 |
| FA §1.1:12 — будочные клетки **gid=1, колонки 23–27, строки 11–15, y=128..208** | M (`measure_cabin`, но только «5 rect’ов») | `python3 /tmp/perfile-figures/cabin_probe.py` (собственный разбор сетки) | 25 клеток, все gid **1**, ряды 11–15, колонки 23–27, y=128..208; колонки 22 и 28 в этих рядах — 0 | **REPRODUCED** | да: сдвиг окна на +1 колонку → 0 клеток gid=1; `row10/row16` в колонках 23–27 = 0 |
| FA §1.1:12 — exclusion **(352,112,96×112)** (y=112..224) | M (закреплён literal) | пересчёт из кода: `TMXLevelRuntime.swift:301-305` + `TMXMapLoader.swift:77-80` | trigger (368,128,32,80) → x=352, y=112, w=max(96,96)=96, h=112 | **REPRODUCED** | да: при `w=100` формула дала бы 164 ≠ 96 (ветка `max(96,…)` различима) |
| FA §1.1:12 / §3:38 — исключение вырезает **именно** будку, не пол | M: `booth_cells_after=0`, `floor_after=1` | `python3 fam_patched.py` (копия M) и свой `solid_cells_intersecting_exclusion` | M: 5→0 и 1→1 ✓; моё: **25** клеток под exclusion, все ряды 11–15 (y=128..208), рядов 17–18 не затронуто (**0**) | **REPRODUCED** | да: мутация порога (ниже на 17 px) прорезает пол → см. следующую строку |
| FA §2:32 — «faithful-мутация: порог ≤95 px прорезает пол» | M (`control_mutated_exclusion`) | M + свой прогон с `y=95, h=129` | M: `control_…_floor_rects_whole=0`; моё: ряд 18 (y=80..96) попадает в exclusion | **REPRODUCED** | да (это и есть must-fail контроль: 0 вместо 1) |
| FA §1.1:12 — «пол под будкой — **две строки y=64..96** (верх 96)» | нет (M проверяет только одну строку, y=80..96) | `cabin_probe.py` → `floor_rows_under_booth` | ряды **18 и 19** = y=80..96 и y=64..80, обе солидные на всю ширину → union y=64..96 | **REPRODUCED** (и это сильнее, чем ассерт M) | да: рядом выше (16, 17 = y=96..128) — пусто, «третьей строки» нет |
| FA §1.1:12 — «запас **ровно 16 px**» | нет | арифметика из тех же rects: 112 (низ exclusion) − 96 (верх пола) | **16 px** | **REPRODUCED** | да: у 4 маркерных кабин тот же запас = **0** (см. ниже) |
| FA §1.1:12 / §9.3:97 / v1-шапка — «**4** маркерные кабины с запасом **0 px**» (`L02S10`,`L03S11`,`L04S16`,`L05S10`; зоны 034/060/090/109) | нет | `cabin_probe.py` → `marker_cabin::*` (ветка `source_marker`/`changing_room`, `TMXLevelRuntime.swift:371-379`) | ровно **4** карты; у каждой `nearest_floor_top == exclusion_bottom` → запас **0 px** (пол цел, касание); `blk_changing_room` = 5 карт всего | **REPRODUCED** | да: `L01S10` (capsule-ветка) даёт 16 px ≠ 0 |
| FA §2:32 / §3:38 — «**полный ASCII-дамп сетки collision (строки 10–21)**» как разрешающий замер | **нет нигде** (в `evidence/` дампa нет; слово «ASCII» только в самих отчётах) | пересобрал сам: дамп рядов 10–21 (см. `cabin_probe.json.collision_rows_10_21`) | цифры отчёта совпали с моей пересборкой | **NO ARTIFACT** (обещанный «разрешающий замер» в репозитории отсутствует; воспроизводимость держится только на более слабом ассерте M) | да (управление = мутация порога) |
| FA §2:32 — эррата «**817/840** … L01S10 уникален (нет завершающих запятых); корректный сплит = ровно 840» | нет | `cabin_probe.py` → `L01S10_*`, `maps_with_missing_trailing_commas` | 840 (=35×24) при `[,\s]+`; 817 при `split(",")` после склейки строк; строк с завершающей запятой в L01S10 — **0 из 24**, у остальных 124 карт — 23 из 24 → файл уникален | **REPRODUCED** | да: 817 ≠ 840; «23 из 24» у всех остальных ≠ 0 |
| FA §1.5:16 — **970 кортежей** в `LEVEL_COMPILER_AUDIT.md` | DR (heredoc из несуществующего `/tmp/docsaudit/docaudit.py`) | `python3 tmx_probe.py` → `doc_tuples` (свой парсер строк `NNN LxxSyy solid= actions=`) | **970**, строк **125**, Σ шапки = 970, поле1 ∈ [0,31] | **REPRODUCED** | да: при фильтре «только зоны 000–049» Σ падает до 383 |
| FA §1.5:16 — **72** точных совпадения по `sourceX/sourceY` | тот же /tmp-heredoc | `tmx_probe.py` → `join_exact_matches` | **72** из **145** объектов (73 мимо) | **REPRODUCED** | да: see next two rows |
| FA §1.5:16 — «контроль **0/370** на нерелевантном индексе» | DR:159 сам говорит «370 из 370 (**кроме 3**) не матчатся» | `tmx_probe.py` → `control_noXY_matched_total` (370 объектов `sourceBlock` без `sourceX/Y`, джойн по `x/16,y/16`) | **3/370** совпало (`blk_anim_pump`→тип 10 ×2, `blk_changing_room`→тип 12 ×1) | **NOT REPRODUCIBLE** (в отчёте округлено до «0», артефакт говорит 3) | да: 3 ≠ 0 |
| FA §1.5:16 — (мой дополнительный контроль устойчивости джойна) | нет | сдвиг `sourceX+1, sourceY+1` и джойн в чужой зоне (`zone+1`) | **0** совпадений и **0** совпадений | **REPRODUCED** (72 — не артефакт смещения) | да: 72 → 0 |
| FA §1.5:16 — «тип 11 = `gunMachine_BOTTOM` **18/18**» | нет (в M нет джойна) | `tmx_probe.py` → `join_families` | `blk_gunMachine_BOTTOM→(11,)` = **18 из 18** объектов (на **15** картах); всего тип 11 в doc — 56 | **REPRODUCED** | да: `blk_tower_rocket` = 23 ≠ 56, `rocket` = 29 ≠ 56 |
| FA §1.5:16 — «тип 15 = конец стадии, не HV» | нет | пропуск 5 объектов `blk_stage_end` через тот же джойн с допуском ±3 клетки | **5/5** дают тип **15** со стабильным `dx=+2, dy=0`; `blk_topdown_electro` = 2 объекта, 0 точных | **REPRODUCED** | да: при точном джойне (без смещения) — 0/5 |
| FA §1.5:16 / §3:39 — «`solid=` не воспроизводится ни при одной дефиниции: **0/125**» | нет | `tmx_probe.py` → `maps_with_solid_equal_doc` (число ненулевых клеток против `solid=` той же зоны) | **0 из 125** | **REPRODUCED** | да: база 125 карт пройдена (не пустой прогон) — `maps_without_collision_layer = 0` |
| FA §1.4:15 — `factory/src` **25 413** LOC Python | нет | `find factory/src -name '*.py' -not -path '*__pycache__*' -print0 \| xargs -0 cat \| wc -l` | **25 413** | **REPRODUCED** | да: с `__pycache__`-путём и без фильтра `*.py` числа меняются |
| FA §1.4:15 — тесты **13 368** LOC | нет | `find factory/tests -name '*.py' … \| xargs -0 cat \| wc -l` | **13 368** | **REPRODUCED** | да |
| FA §1.4:15 — policy `adaptive_grok` **21 071** LOC, весь `.grok-stack` **21 105** | нет | два `find` по `.grok-stack/adaptive_grok` и `.grok-stack` | **21 071** и **21 105** (дельта 34 строки вне `adaptive_grok`) | **REPRODUCED** | да: 21 105 ≠ 21 071 |
| FA §1.4:15 — оверлей «в ~**14** раз больше продукта» | нет | (25 413+13 368+21 071) / Swift-LOC(`find Exolon -name '*.swift'`) = 59 852 / 4 227 | **14.16×** | **REPRODUCED** (определение: все Python-оверлеи против Swift) | да: без тестов → 11.0×; против TMX-данных → другое число |
| FA §1.4:15 / §5:66 — **201/201 OK** в чистом disposable-контуре | `factory/tests/run_disposable_exit.py` | `python3 factory/tests/run_disposable_exit.py` (лог `/tmp/perfile-figures/exolon-factory-exit-mine.log`) | `Ran 201 tests in 160.689s` / `OK` / `PASS: disposable PostgreSQL …`, rc=0, **0 skipped** | **REPRODUCED** | да: без БД тот же `discover` → `OK (skipped=71)` |
| FA §2:29 / §5:66 — «первые живые **71** postgres-интеграционных теста» | `…/test_postgres_integration.py` | `UV_PROJECT_ENVIRONMENT=/tmp/… uv run --frozen --project factory python -m unittest discover -s factory/tests -t .` | `Ran 201 tests … OK (**skipped=71**)` | **REPRODUCED** | да: 71 skip без БД ↔ 0 skip с БД |
| FA §1.4:15 / §5:66 — «PostgreSQL **17.11** через docker» | `run_disposable_exit.py` (пин `postgres:17-alpine`) | `docker run --rm --entrypoint /bin/sh postgres:17-alpine -c 'postgres -V'` | `postgres (PostgreSQL) 17.11` | **REPRODUCED** | да: в кэше есть 15/16/17.5/17.6 — другая версия дал бы другую строку |
| FA §5:66 — «три прогона: FAIL(251 s, 1 ERROR)/OK(173 s)/OK(163 s)» + §1.4 «**1/3** невоспроизводимый ERROR» | названные логи `/tmp/exolon-factory-exit{,2,3}.log` | `ls /tmp/exolon-factory-exit*.log` | таких файлов **нет** (есть только чужой `exolon-factory-exit-fresh.log`); мои 2 прогона: 160.689 s OK и 158.347 s OK | **NOT REPRODUCIBLE** (per-run тайминги и 1 ERROR; артефакты удалены) — существо flaky подтверждено внешне: `gh issue view -R Dimkox/adaptive-grok-build-pro 155` → OPEN, заголовок «…make mandatory postgres evidence **load-flaky (1/3)**» | частично да: внешний артефакт (#155) есть, локального — нет |
| FA §10:110 — «Логи factory-контуров: `/tmp/exolon-factory-exit{,2,3}.log`» | — | `ls -la /tmp/exolon-factory-exit*.log` | цитируемых файлов нет ( `/tmp` — не репозиторный носитель) | **NO ARTIFACT** (ссылка битая по построению) | да: ссылка не разрешается |
| FA §1.4:15 — «прошлый «100 тестов / 3 ERROR» закрыт обновлением factory (`ModuleNotFoundError` исчез вместе с b8aee42)» | `AR/repo_explorer-fullaudit.md:61-63` + delta | `PYTHONPATH=.:factory python3 -B -c "import factory.tests.test_execution_contracts, factory.tests.test_postgres_integration"` и `git diff --name-status 403eb13 b8aee42` | `IMPORTS OK`; оба файла добавлены в `b8aee42`; прогон 201/201 без ошибок импорта | **REPRODUCED** | да: «Ran 100 … FAILED (errors=3)» остаётся только как цитата в evidence (первичный receipt перезаписан 03:07) |
| FA §5:67 — классификатор: `repo.languages: []`, `kind: generic`, `quality_profiles: [base]` | `.grok-stack/adaptive_grok/repo.py` + `.grok-stack/runtime/active-route.json` | `python3 -c "from adaptive_grok.repo import detect_repo; print(detect_repo(Path('.').resolve()).to_dict())"` и чтение `active-route.json` | `{'kind':'generic','languages':[], …}`; в роуте `route_id=7db1f3f0b126`, `quality_profiles:['base']`, `repo.kind:'generic'`, `languages:[]` | **REPRODUCED** | да: `detect_repo(<repo>/factory)` → `{'kind':'python','languages':['python'],…}` |
| FA §5:67 — «профиля Swift нет в `quality-profiles/` (ai/bitrix/contracts/data/frontend/infra/integration/php — без даже python)» | `.grok-stack/config/quality-profiles/` | `ls -1 .grok-stack/config/quality-profiles/` | 9 файлов: `ai base bitrix contracts data frontend infra integration php` — ни `swift.json`, ни `python.json` | **REPRODUCED** | да: `base.json` существует (в перечне FA его нет, но FA и не заявлял полноту) |
| FA §5:65 — `factory/contracts` = «**28** jsonschema + **7** openapi» | нет | `find factory/contracts -type f` по подкаталогам | `jsonschema/` = **27**, `openapi/` = **7** ✓, `schemas/` = **4** (всего 38) | **NOT REPRODUCIBLE** (28 ≠ 27; и ≠ 31 если слить `schemas/`) | да: три разных определения — ни одно не даёт 28 |
| FA §5:65 — «загрузчиков из `src` почти не касается» | нет | `grep -rn "contracts/" factory/src --include=*.py \| wc -l`; `grep -rn jsonschema factory/src` | **0** обращений к `contracts/` из `src` (упомянуто 1 имя схемы в строке данных) | **REPRODUCED** (фактически «совсем не касается») | да: тот же grep по `factory/tests` даёт непустой список |
| FA §5:68 — «`factory/README.md`: **7 из 8** относительных ссылок битые» | нет | свой resolver: `re.findall(r'\]\(([^)]+)\)')`, base=`factory/` | битых **7** ✓ (числитель сходится), но всего относительных ссылок **10 уникальных / 11 вхождений** | **NOT REPRODUCIBLE** (знаменатель: 8 не восстанавливается ни при одном определении) | да: base=корень репо → 10 из 10; только `../`-ссылки → 6 из 7 |
| FA §5:68 — версии «v2.0.13 / v2.0.14 / `pyproject 0.1.0` / `product_version 2.0.18` в одном дереве» | `factory/README.md`, `factory/pyproject.toml`, `engineering/runbooks/factory-source.json` | `grep -n "v2\.0\.1[34]" factory/README.md`; `grep -n version factory/pyproject.toml`; JSON-ключ `product_version` | README:14 `v2.0.13`, README:16 `v2.0.14`, `pyproject 0.1.0`, manifest `product_version=2.0.18` | **REPRODUCED** | да: все четыре значения в разных файлах (не дубли одного места) |
| FA §5:69 — дельта `403eb13..b8aee42`: «**24 файла, +1477/−4**, продукт не тронут» | git | `git diff --shortstat 403eb13 b8aee42`; `git diff --stat 403eb13 b8aee42 -- Exolon Exolon.xcodeproj README.md ORIGINAL_MECHANICS.md LEVEL_COMPILER_AUDIT.md` | `24 files changed, 1477 insertions(+), 4 deletions(-)`; дельта по продукту **пуста** | **REPRODUCED** | да: `git diff --numstat` по одной только ветке docs даёт другую пару чисел |
| FA §5:69 — «**добавлен** один src-файл (`landing_http.py`) + 2 тест-модуля» | git | `git diff --name-status 403eb13 b8aee42` | `M factory/src/adaptive_factory/landing_http.py` (изменён, не добавлен); `A factory/tests/test_execution_contracts.py`, `A factory/tests/test_execution_service.py` | **NOT REPRODUCIBLE** (как сформулировано: src-файл **не** новый) | да: статус A vs M различим |
| FA §5:69 — «sha256 `landing_http.py` ровно манифестный» | `engineering/runbooks/factory-source.json` | `sha256sum factory/src/adaptive_factory/landing_http.py` vs `changed_managed_files` | `21c03d67…01ddc` = манифестному | **REPRODUCED** | да: `changed_managed_files` содержит ровно 1 запись |
| FA §5:69 — «`managed_file_count: 346` **воспроизводится**» | `engineering/runbooks/factory-source.json` (только поле) | `grep managed_file_count` + поиск производителя: `grep -rn "managed_file_count" factory/src .grok-stack scripts` (пусто) + пересчёты дерева: `factory`=146, `.grok-stack` (без runtime/pycache)=65, `scripts`=13, корневые .py=9, `.agents`=30, `.grok`=75 | поле = **346** ✓ как запись; независимый пересчёт **не** восстанавливается ни одной суммой (233/263/338/…) | **NO ARTIFACT** для слова «воспроизводится» (артефакт = само утверждение в манифесте) | да: ни одна комбинация подкаталогов не даёт 346 |
| FA §1.6:17 / §6:75 — «корневого `.gitignore` нет вовсе» | filesystem | `ls .gitignore`; `find . -name .gitignore -not -path './.git/*'` | в корне — нет; есть только вложенные `factory/.gitignore`, `.ruff_cache/.gitignore` | **REPRODUCED** (формулировка «корневого» точна) | да |
| FA §1.6:17 / §6:75 — «**8** untracked-записей `git status`» | git | `cp .git/index /tmp/… && GIT_INDEX_FILE=… git read-tree b8aee42 && git status --porcelain \| grep -c '^??'` | на дереве `b8aee42` = **9**; минус `.qwen/` (ctime 02:16:59, т.е. позже коммита отчёта 02:01:33) = **8** ✓; на текущем HEAD = **9** (и уже 11 к моменту правки — чужие полосы пишут в `evidence/`) | **REPRODUCED** с оговоркой (снимок; счётчик живёт в mutable-состоянии) | да: добавление/удаление одного untracked-файла меняет число |
| FA §1.6:17 / §6:75 — «доказательства первичного аудита живут только в рабочем дереве (untracked)» | git | `git ls-tree -r --name-only b8aee42 -- engineering` vs `git ls-files engineering/reports` | на `b8aee42` — пакета 7db1f3 и `engineering/reports/` в индексе нет ✓; на `52795d1` — **оба отчёта и 27 файлов пакета закоммичены** (`bcacf30`) | **REPRODUCED** для заявленного HEAD / **устарело** на текущем HEAD | да: `git ls-files` на двух коммитах даёт 0 vs 29 |
| FA §6:76 — «`AGENTS.md` ссылается на отсутствующие `START_HERE.md`, `PROJECT_STATE.json`, `mistakes.md`, `VERSION`, `architecture/*`, `trust-ci/`, `scripts/install_into.py`» | AGENTS.md | цикл `[ -e "$p" ]` по всем 7 путям | все 7 — **MISSING** (`decisions.md` существует — и FA его не требует) | **REPRODUCED** | да: контроль «существующий» `decisions.md` не попал в список |
| FA §6:77 — «`engineering/adr`, `engineering/reviews`, `engineering/contracts/{openapi,asyncapi,schemas}` — пусто» | filesystem | `for d in …; do find "$d" -mindepth 1 \| wc -l; done` | 0, 0, 0, 0, 0 | **REPRODUCED** | да: `engineering/changes` (соседний каталог) = непустой → метод различает |
| FA §6:78 — «`b8aee42` на remote под именем `codex/update-factory-20260920` → открыт **PR #1 (DRAFT)**» | git + GitHub | `git log --oneline -4` (метка `origin/codex/update-factory-20260920`) и `gh pr view 1 --repo Dimkox/exolon --json number,state,isDraft,headRefName` | ref на месте; PR: `state=OPEN`, `isDraft=true`, `headRefName=codex/update-factory-20260920` | **REPRODUCED** (внешний носитель проверен живым запросом) | да: `isDraft=false` у любого другого PR изменило бы вердикт |
| FA §6:78 — «текущая локальная ветка — **неотправленный** однофамилец; upstream-трекинга нет ни у одной локальной ветки» | git | `git branch -vv` | на `52795d1`: `codex/factory-updated-20260920 [origin/codex/factory-updated-20260920]` — ветка **отправлена и отслеживается**; треккинга нет только у 2 из 3 | **NOT REPRODUCIBLE** на текущем HEAD (было верно на `b8aee42`; проверяемое состояние измен后) | да: `git branch -vv` различает 0 и 1 tracking |
| FA §6:78 — «**два** внешних worktree в `~/.exolon-factory-update-20260920`» | git | `git worktree list`; `ls -d ~/.exolon-factory-update-20260920` | ровно **2** внешних worktree ✓, но путь — `<worktree>` (`~/.exolon…` не существует) | **REPRODUCED** по числу / **NOT REPRODUCIBLE** по пути | да: `ls ~/.exolon…` → No such file |
| FA §6:79 — «shared `.xcscheme` отсутствует» | filesystem | `find Exolon.xcodeproj -type f` | ровно 1 файл — `project.pbxproj`, `xcshareddata/` нет | **REPRODUCED** | да: `find … -name '*.xcscheme'` = 0 |
| FA §1.6/§6:80 — «XCTest у игры: **0** файлов» | нет | `grep -rln "XCTest" --include=*.swift .` ; `grep -c Test Exolon.xcodeproj/project.pbxproj`; `find . -name '*Tests*.swift'` | 0 swift-файлов с XCTest, 0 упоминаний `Test` в pbxproj, 0 файлов `*Tests*.swift` | **REPRODUCED** (негативная заявка подтверждена) | да: совпадения `XCTest` есть только в .md — другой тип файлов |
| FA §4:49 (B-01) — «**35 exact** / **90** без локального пола (125)» | M + `B01J` | `python3 fam_patched.py` → `b01_spawn.tally`; побайтовый `diff` перегенерированного JSON с `B01J` | `exact=35`, `void=89`, `16_above=1`, total=125; JSON **идентичен** коммитнутому | **REPRODUCED** (35 и 90=125−35) | да: мой независимый контроль — подмена spawn из чужой карты → `exact=27` (≠35) |
| FA §4:49 / §9.4:98 — «остальные **90** добираются глобальным `fallbackGroundY`», «90 **без локального пола**» | `B01J` (тот же файл) | чтение категоризации `B01J` + `cabin_probe.py` | **89** карт без локального пола; **1** карта (`L03S11.tmx`, зона 060) имеет локальный пол в 16 px под ногами | **NOT REPRODUCIBLE** в такой формулировке (переоценка на 1: «без пола» = 89) | да: 89 ≠ 90; карта названа |
| FA §4:49 — B-01 независимая разбивка «★ моя: 35 exact / 90 — сходится» | M | ещё одно определение: опора в пределах 46-пиксельного movement-box вместоцентральной колонки | **35 exact / 89 void / 1 off** — то же число | **REPRODUCED** (определение не влияет) | да: box-определение ≠ center-определение по координатам, но не по итогу |
| FA §4:50 (B-02) — «в `L05S16` вся коллизия — **один** rect x=0..368, колонки 31..34 пусты» | нет | `cabin_probe.py` → `L05S16_*` (тот же алгоритм слияния, что `TMXTileMapRenderer`) | **1** rect `(0, 48, 368, 16)`; ни одной солидной клетки в колонках 31–34 | **REPRODUCED** | да: на `L01S10` тот же код даёт 60+ rect'ов |
| FA §4:50 — «`isGrounded` требует `footY > groundY+1.5`» | нет | `grep -n "1\.5" Exolon/GameCore/Player/Player.swift` | `Player.swift:198` `if !supported && footY > fallbackGroundY + 1.5`; `:192` `abs(solid.maxY - footY) <= 1.5` | **REPRODUCED** | да: константа 1.5 различима от 0/16 |
| FA §4:51 (B-03) — «бонус до проверки пустого `nextLevel`; `lives*1000`, refill; `GameScene.swift:612,708-717,761-767`» | нет | чтение cited ranges | `:612` `applyOriginalStageBoundaryIfNeeded(...)` **до** `guard !next.isEmpty` (`:614`); `:626-630` `awardPoints(gameState.lives * 1_000)` + refill; `:708-717` `beginFromTitle` без сброса позиции/уровня; `:761-767` `showTitleAfterContentComplete` → `.title` | **REPRODUCED** (все 3 ссылки точны; игровой эффект остаётся play-гипотезой — FA так и пишет) | да: комментарий «deliberately dormant» (`:623-624`) соседствует с живым `guard` (`:626`) |
| FA §4:52 (B-04) — «★ проверил: **ни один** из 125 TMX не сжат» | M (`collision_gids` читает `compression`, но не считает) | `tmx_probe.py` → `maps_compressed_collision` (python `zlib`) | **0** карт с `compression` (из них 4 с base64-слоем) | **REPRODUCED** | да: тот же декодер на base64-картах проходит ветку, а на сжатой упал бы |
| FA §4:52 — «любой `compression` → throw → `fatalError`», `TMXMapLoader.swift:305-333`, `TMXLevelRuntime.swift:53` | нет | `sed -n '305,335p' TMXMapLoader.swift`; `sed -n '50,56p' TMXLevelRuntime.swift` | `:307-308` `throw unsupportedCompression`; `:53` `fatalError("Unable to load TMX map …")` | **REPRODUCED** | да: обе ссылки указывают ровно на эти строки |
| FA §4:52 — «`makeMap()` не требует непустоты карты (well-formed XML с чужим корнем = играбельная пустота)» | нет (линк не дан) | `sed -n '104,168p' TMXMapLoader.swift` | `load()` проверяет только `delegate.error`/`parser.parse()`; `makeMap()` (`:155`) без guard на `width/height/layers`; делегат реагирует лишь на имена элементов → чужой корень = пустой `TMXMapData` | **REPRODUCED** (чтением кода; исполнение Swift на Linux невозможно) | да: `<map>` vs иной корень различается делегатом |
| FA §4:53 (B-05) — «★ grep: **0** вызовов `GamePersistence.loadCheckpoint()`», «**7** ключей `Exolon.Step10.*`», `GameState.swift:24-31` | нет | `grep -rn "loadCheckpoint" Exolon`; `grep -c 'Exolon\.Step10\.' Exolon/GameCore/GameState.swift` | 1 вхождение = само определение (`:40`), **0** вызовов; **7** ключей в `enum Key` (`:25-31`) | **REPRODUCED** (обе негативные заявки) | да: control-поиск по `saveHighScore` даёт ненулевые вызовы тем же методом |
| FA §4:54 (B-06) — «`windowDidResignKey` чистит только `.keyboard`», `GameView.swift:42-44`; «Cmd+Q не прокинут» | нет | `sed -n '40,46p' GameView.swift`; `grep -rn "terminate\|performKeyEquivalent\|MenuItem" --include=*.swift Exolon` | `:42-44` ровно `inputState?.reset(source: .keyboard)`; из terminate-механики только `applicationShouldTerminateAfterLastWindowClosed` (`AppDelegate.swift:39`), меню/key-equivalent — **0** | **REPRODUCED** | да: gamepad-источник в reset не входит (видимо в том же блоке) |
| FA §4:55 (O8) — «**19** объектов на **18** картах»; «`isActive` меняется только при сборе бонуса»; `LevelObstacles.swift:699,718-723,431-478` | нет | `tmx_probe.py` (double_launcher) + чтение кода | `double_launcher` = **19 объектов / 18 карт** ✓; `:699` `guard isActive, …`; `:718-723` `collectBonusIfTouched` ставит `isActive=false`, alpha 0.45 ✓; в `GameScene.swift` `doubleLaunchers` **не упоминается вовсе** (мишени пуль/гранат: turrets, destructibleObstacles, incubators, terrain) ⇒ ни бластер, ни граната не убивают ✓ | **REPRODUCED** по числам и по сути | да: `turrets`/`incubators` в тех же списках есть, `doubleLaunchers` — нет |
| FA §4:55 (O8) — **ссылка** `LevelObstacles.swift:431-478` как место «нет в списках destructibles» | нет | `sed -n '425,480p' LevelObstacles.swift` | этот диапазон = класс `BubbleEnemy` (`node`, `points = 150`, `fixedUpdate`) — приписанного факта **не содержит**; правильный локатор: `GameScene.swift:407-415,437-454` | **NOT REPRODUCIBLE** (битая цитата) | да: по именам классов диапазон различим |
| FA §4:56 / §3:41 (O7) — «**10/10** карт с beam-маркерами имеют обе ветки»; «10 карт × 2 поля × **25** хитов» = 50 | M (`measure_beams`) | `python3 fam_patched.py` (assert 10/10) и свой `tmx_probe.py` (независимый разбор) | M: `any=10`, `both=10`, список 10 карт ✓; моё: `blk_beam_up=10`, `blk_beam_down=10`, карт с обеими = **10**; `hitPoints = 25` (`LevelObstacles.swift:766`), поле создаётся на **каждый** из 20 маркеров (`TMXLevelRuntime.swift:356-360`) | **REPRODUCED** | да: мой контроль «только up» или «только down» даёт 0 карт |
| FA §9.6:100 — «список beam-пар в `evidence/fullaudit_measurements.py`» | M | `grep -n '"list"' M` + запуск | literal-списка в файле нет, но скрипт его **печатает** (10 карт, зона 035 = `L02S11.tmx` в списке) | **REPRODUCED** (с оговоркой: список производный, не константа) | да: без запуска список недоступен |
| FA §4:58 (D-02) — «**5** самых первых играемых зон (**000-003, 006**) без `imagelayer`» | DR (heredoc) | `grep -L imagelayer Exolon/Resources/*.tmx` и свой разбор + соответствие `zoneNumber` | **5** файлов: `L01S01,L01S02,L01S03,L01S04,L01S07` = зоны **000,001,002,003,006** — ровно как заявлено | **REPRODUCED** | да: с контролем по всем 125 (`maps_without_imagelayer`) остальные 120 имеют слой |
| FA §4:59 (D-03) — «`coordinateMode=tiledRect` ровно в **1/125** файлов» | нет | `grep -rl coordinateMode Exolon/Resources/*.tmx`; `tmx_probe.py` | **1** (`L01S10.tmx`); в Swift — единственное потребление: `TMXMapLoader.swift:77` | **REPRODUCED** | да: 124 файла без него |
| FA §4:59 (D-03) — «README подаёт конвертацию rect как системное свойство» (охват) | DR | `tmx_probe.py` → объекты с `width>0 and height>0` | **360** прямоугольных объектов, режим объявлен у **1** | **REPRODUCED** | да: 1 ≠ 360 |
| FA §4:59 (D-03) — «две геометрии exclusion: `max(96,w+64)` vs `w+32`», `TMXLevelRuntime.swift:304,377` | нет | `sed -n '304p;377p'` | `:304` `width: max(96, trigger.width + 64)`; `:377` `width: trigger.width + 32` | **REPRODUCED** (линии точны) | да: формулы различимы |
| FA §4:61 — «`try!`/`as!` — во всём дереве ноль» | нет | `grep -rn "try!\| as!" --include=*.swift . \| grep -v '^./.git'` | **0** | **REPRODUCED** (негатив) | да: тот же grep по `-B` артефактам .md даёт совпадения в тексте — метод различает |
| FA §4:61 — «туннелирование: шаг **≤5 px** против 16-пиксельных клеток» | AR:504-506 (наблюдение, без скрипта) | `grep -n "moveSpeed\|maximumFallSpeed\|fixedTimeStep" Player.swift GameConstants.swift` → `300/60` | максимум **5.0 px/шаг** по Y (`maximumFallSpeed=-300`, `fixedTimeStep=1/60`) и 1.5 px по X (`moveSpeed=90`) | **REPRODUCED** | да: без clamp `gravity*dt` накапливалось бы >5 px после 50 шагов |
| FA §4:61 — «ловушка «под потолком 52..63 px»: зазоры кратны 16» | AR:512 (распределение «48→91, 64→59», не коммитилось как инструмент) | свой прогон: вертикальные пустые прогоны коллизий по всем 125 картам | прогонов в окне (52,63] = **0**; не кратных 16 = **0**; топ-длины 64 (2625), 32 (506), 48 (453) | **REPRODUCED** по нулю в окне; распределение архитектора (**91/59**) — **NOT REPRODUCIBLE** (другая единица: «клетки», а не прогоны) | **нет** (дегенеративный контроль): длинный прогон клеток кратен 16 **по построению**, поэтому «окно 52..63» не могло и не перевернуться → зонд не информативен |
| FA §7:84 — «O8 (19 объектов на 18 картах) · O7 (50 хитов вместо 25, 10 карт) · B-01 (90/125) · O1 нижние пушки (**18**, walkthrough-зона 023)» | нет | `tmx_probe.py` + `B01J` + чтение | 19/18 ✓ · 10 карт ✓ · 90/125 ✓ · `blk_gunMachine_BOTTOM` = **18** объектов, `L01S24.tmx` имеет `zoneNumber=023` и содержит BOTTOM ✓ | **REPRODUCED** | да: тот же запрос без фильтра по sourceBlock даёт 127 `source_marker` |
| FA §7:86 — «V2: **три** «номера шага» (окно Step 9, HUD STEP 9…, `Exolon.Step10.*`)» | нет | `grep -rn "STEP \|Step 9\|Step 10" --include=*.swift Exolon` | **4** разных носителя: окно `Step 9 Rebase` (`AppDelegate.swift:16`), HUD `STEP 9 · …` (`GameScene.swift:67,656,984`), титульный оверлей `STEP 10` (`GameScene.swift:832`), ключи `Exolon.Step10.*` | **NOT REPRODUCIBLE** (число «три» занижено; v1:265 сам цитирует `STEP 10` на `GameScene.swift:832`) | да: строка 832 существует и не входит в перечисленные три |
| FA §8:90 — «`Swift 6.x Release Signing Key` **истёк 16.09.2026**»; «живой ключ один — Automatic Signing Key #4»; «на keyserver обновлённой нет» | нет (гостевая GNUPG) | `gpg --list-keys \| grep -iA1 swift` по локальной связке (импортирована 20.09 00:40) + `gpg --keyserver hkps://keyserver.ubuntu.com --recv-keys 52BB…981F` | `pub rsa4096 2024-09-16 [expired: 2026-09-16]` «Swift 6.x Release Signing Key» ✓; единственный живой — `Swift Automatic Signing Key #4 … [expires: 2027-11-14]` ✓; keyserver: «**not changed**» (обновлённой нет) ✓ | **REPRODUCED** | да: в связке 7 просроченных ключей Swift и ровно 1 живой |
| FA §8:90 — «подпись релиза **создана 14.09.2026**, PGP **Good**» | нет (`.sig`/`.tar.gz` на диске отсутствуют) | `find /opt /tmp -maxdepth 3 -iname '*swift*.sig'`; 4 попытки `download.swift.org/…/swift-6.4-release/…` и `…/security/all-keys.asc` | все кандидаты **404**, `.sig` нет → сам факт «Good signature» в этом прогоне не перепроверяется | **NO ARTIFACT** | нет (пробить нечем) |
| FA §8:91 — «`run_disposable_exit.py` не имеет `--help`: неизвестный аргумент молча игнорируется и **запускается полный прогон**» | `factory/tests/run_disposable_exit.py` | `grep -n "argparse\|sys.argv" runner` (пусто) + реальный запуск `python3 factory/tests/run_disposable_exit.py --help-mode-does-not-exist` | аргументов нет вообще; прогон с выдуманным флагом дошёл до `Ran 201 tests in 158.347s / OK / PASS`, rc=0 | **REPRODUCED** | да: `--help` ведёт себя как запуск без аргументов (отличное от ожидаемого «usage/exit 2») |
| FA §8:91 — «SIGPIPE-прерывание оставляет хвостовые venv в `/tmp`» | — | не проверял (намеренно: требует убивать прогон и мусорить в `/tmp`); после 2 завершённых прогонов `ls -d /tmp/adaptive-factory-exit-venv-*` = **0** | — | **NOT REPRODUCIBLE** (не тестировалось; обратного свидетельства нет) | н/т |
| FA §10:108 / §5 — «`python3 …/fullaudit_measurements.py` … **rc=0 ⇔ все числа совпадают с отчётом**» | M | `python3 /tmp/perfile-figures/fam_patched.py` | rc=**0**, `ALL_MEASUREMENTS_MATCH_REPORT` | **REPRODUCED** с существенной оговоркой: ассерты M покрывают только cabin(5/1/0/1/0), beams(10/10) и b01(total=125, exact=35). Числа «**90** без пола», «**4** кабины с 0 px», «**0/370**», «**360** rect», «**25 хитов**», «**19/18**» скриптом **не** проверяются | да: при подмене порога (`tally["exact"]==36`) M честно возвращает rc=1 |
| FA §9.4:98 — «зоны спавна из `evidence/fullaudit-b01-spawn.json` (**90** без локального пола)» | `B01J` | `python3 -c "Counter(json.load(open('B01J')).values())"` | файл говорит `void=89`, `16_above=1`, `exact=35` → «90 без пола» приписано файлу, которого в файле нет | **NOT REPRODUCIBLE** (битая атрибуция: см. строку B-01 выше) | да: 89 ≠ 90 |
| v1-шапка (коррекция) — «формула `minY - 16` подтверждена как код» | нет | `sed -n '301,305p' TMXLevelRuntime.swift` | `y: max(0, trigger.minY - 16)` ✓ | **REPRODUCED** | да |
| v1-шапка — «будочные клетки y=128..208 вырезаются исключением — по замыслу; пол y=64..96 (верх 96) нетронут с запасом 16 px (граница 112)» | M | `cabin_probe.py` (см. 5 строк про cabin выше) | точно: вырезано 25 клеток ряда 11–15, пол (ряды 18–19) не затронут, зазор 16 px | **REPRODUCED** | да (мутация порога прорезает пол) |
| v1-шапка — «C1 понижён до P3: у **4** маркерных кабин запас **0 px**» | нет | `cabin_probe.py → marker_cabin::*` | 4 карты, запас 0 px | **REPRODUCED** | да |
| v1-шапка — «§9.1 неверна системно: тип 11 ≠ ракетницы, тип 15 ≠ HV, `solid=` не сходится 0/125» | нет (DR-heredoc) | `tmx_probe.py` + пропуск stage_end | 11 = BOTTOM 18/18 (ракетные башни = 23/29, ≠ 56); 15 = stage_end 5/5 (HV = 2 объекта, 0 совпадений); solid= **0/125** | **REPRODUCED** | да (см. отдельные control-строки) |
| v1-bl-шапка — «строки **C1/C1b (P0) сняты**» | — | сверка с FA §4/§7 и таблицей §3 | C1 → P3 в FA §4:59 (§7 P3 «C1-demoted»), C1b отсутствует в v2-бэклоге; в v1-бэклоге обе P0-строки физически ещё в таблице, но закрыты шапкой | **REPRODUCED** (категориальная) | да: в v1-таблице C1/C1b помечены P0 — шапка их перекрывает |
| FA §3:41 (O7) — «моя независимая проверка 10/10» | M | уже посчитано дважды (M + мой парсер) | совпадает | **REPRODUCED** | да |
| FA §3:43 (V1/V2) — «ключи `UserDefaults` = `Exolon.Step10.*` (`GameState.swift:25-31`)» | нет | `sed -n '25,31p' GameState.swift` | 7 ключей с префиксом `Exolon.Step10.` в `enum Key` (`:24-31`) | **REPRODUCED** | да |
| FA §4:49 (B-01) — ссылки `Player.swift:291-297`, `TMXLevelRuntime.swift:60-66` | нет | `sed -n '291,297p' Player/Player.swift`; `sed -n '59,68p' TMXLevelRuntime.swift` | `:291-297` = `landOnFallbackFloorIfNeeded` ✓; `:59-66` = сборка `playerBottom`/`resolvedSpawnCenter` из `vitorc` ✓ | **REPRODUCED** | да |
| FA §1:3 — «продукт идентичен `403eb13`» | git | `git diff --stat 403eb13 b8aee42 -- Exolon Exolon.xcodeproj` | пусто → идентичен | **REPRODUCED** | да: тот же diff по всему дереву даёт 24 файла |

## Итог

Всего строк в таблице замера: **94** (каждая строка = одна цифровая или категорийная заявка
отчёта либо одна отдельная проверка приписанной ссылки/артефакта).

| Вердикт | Кол-во |
| --- | ---: |
| **REPRODUCED** | **77** |
| **NOT REPRODUCIBLE** | **12** |
| **NO ARTIFACT** | **5** |

Семь REPRODUCED-строк воспроизведены **с частичной оговоркой** (в самой строке таблицы помечено):
`§1.6:17/§6:75` «8 untracked» (снимок: сегодня 9 и 11 — счётчик живёт в mutable-состоянии),
`§1.6:17/§6:75` «доказательства только в рабочем дереве» (верно для `b8aee42`, на `52795d1` уже неверно),
`§3:40` «27 имён `SKTexture(imageNamed:)`» (число 27 сходится только на широкой метрике; буквально по формулировке — 17),
`§6:78` «два внешних worktree» (число ✓, путь в отчёте `~/.exolon…` — неверен),
`§4:61` «зазоры кратны 16» (ноль в окне (52,63] ✓, распределение архитектора 48→91/64→59 — не моя единица измерения),
`§9.6:100` «список beam-пар в скрипте» (скрипт его печатает, literal-списка в файле нет),
`§4:61` «ловушка под потолком» — **здесь контроль не переворачивается**: длина прогона целых клеток кратна 16 по построению,
поэтому зонд «окно 52..63 px» не может дать иное число и не информативен как подтверждение (единственный невалидный контроль в этом прогоне).

**NOT REPRODUCIBLE (12):**
1. `§1.2:13`/`§2:30` — метка «Swift **6.4.0**»; тулчейн печатает `Swift version 6.4 (swift-6.4-RELEASE)`.
2. `§1.5:16` — «контроль **0/370**»: артефакт (`DR:159`) и мой замер дают **3/370** (направление контроля сохранено, число завышено).
3. `§5:66` + `§1.4:15` — тайминги трёх прогонов (251/173/163 s) и «1 ERROR под загрузкой (1/3)»: логи в `/tmp` удалены; мои прогоны — 160.689 s OK и 158.347 s OK; flaky подтверждён только внешним носителем (issue **#155** OPEN, заголовок содержит «load-flaky (1/3)»).
4. `§5:65` — «**28** jsonschema»: в `factory/contracts/jsonschema/` **27** (плюс 4 в `schemas/`, 7 в `openapi/`); ни одно определение не даёт 28.
5. `§5:68` — «**7 из 8** относительных ссылок»: битых 7 ✓, но относительных ссылок **10 уникальных / 11 вхождений** (при base=корень — 10 из 10 битых; только `../`-ссылки — 6 из 7).
6. `§5:69` — «**добавлен** один src-файл `landing_http.py`»: в дельте он `M` (изменён); `A` — только 2 тест-модуля.
7. `§6:78` — «upstream-трекинга нет ни у одной локальной ветки / текущая ветка — неотправленный однофамилец»: на `52795d1` ветка отправлена и отслеживает `origin/codex/factory-updated-20260920` (`git branch -vv`).
8. `§4:49`/`§9.4:98` — «остальные **90** … **без локального пола**»: без пола **89**, 1 карта (`L03S11.tmx`, зона 060) имеет локальный пол в 16 px под ногами.
9. `§4:55` (O8) — приписка «`LevelObstacles.swift:431-478`» как место «нет в списках destructibles»: этот диапазон — класс `BubbleEnemy`; верный локатор `GameScene.swift:407-415,437-454`.
10. `§7:86` — «**три** „номера шага“»: носителей **4** (окно `Step 9`, HUD `STEP 9` ×3 строки, титульный оверлей `STEP 10` на `GameScene.swift:832`, ключи `Exolon.Step10.*`).
11. `§8:91` — «SIGPIPE-прерывание оставляет хвостовые venv в `/tmp`»: намеренно не проверялось (требует убивать прогон и мусорить); после двух завершённых прогонов `ls -d /tmp/adaptive-factory-exit-venv-*` = **0**.
12. `§9.4:98` — атрибуция «90 без локального пола» файлу `evidence/fullaudit-b01-spawn.json`: в самом файле `void=89`, `16_above=1`.

**NO ARTIFACT (5):**
1. `§2:32`/`§3:38` — «полный ASCII-дамп сетки collision (строки 10–21)» как разрешающий замер: в `evidence/` такого файла нет (дамп пересобран мной, цифры отчёта совпали).
2. `§2:28` — «снятие **9** ModuleNotFoundError-ошибок»: в артефактах их **3** (`AR/repo_explorer-fullaudit.md:300`, `v1:48`); «9» не встречается нигде.
3. `§5:69` — «`managed_file_count: 346` **воспроизводится**»: значение подтверждается как **поле** манифеста; независимого пересчёта в дереве нет (ни один инструмент не вычисляет это число; мои суммы по подкаталогам — 233/263/338).
4. `§10:110` — «Логи factory-контуров: `/tmp/exolon-factory-exit{,2,3}.log`»: цитируемых файлов нет; носитель вне репозитория, ephemeral.
5. `§8:90` — «подпись релиза **создана 14.09.2026**, PGP **Good**»: `.sig`/тарбол на диске отсутствуют, 4 кандидата URL на `download.swift.org`/`swift.org` → 404 (истечение ключа 16.09.2026, «живой ключ один» и «на keyserver обновлённой нет» — воспроизведены, см. строку выше).

**Состояние воспроизводимой базы отчёта.** Единственный закреплённый измеритель продукта —
`evidence/fullaudit_measurements.py` + `evidence/fullaudit-b01-spawn.json`; он закрывает лишь cabin
(5/1/0/1/0), beams (10/10) и b01 (total 125, exact 35). Заявки «90 без пола», «4 кабины с 0 px»,
«0/370», «360 rect», «25 хитов», «19/18», «1/125 tiledRect», «5 без imagelayer», «0 вызовов
loadCheckpoint», «18 BOTTOM», «solid= 0/125» этим скриптом **не** проверяются: они порождены heredoc'ами из
несуществующих `/tmp/docsaudit/*.py` (тексты команд — inline в evidence). Побайтовые сверки закреплённых
артефактов: перегенерированный `fullaudit-b01-spawn.json` **идентичен** коммитнутому; перегенерированный
`linux-static-audit.json` **идентичен** коммитнутому; `rc=0 ⇔ все числа совпадают с отчётом` подтверждён (rc=0),
но с оговоркой: при `tally["exact"]==36` скрипт честно вернул бы rc=1 — ассерты узкие, а не «все числа отчёта».

**Вердикт: pass**

Ни одна P0/P1-заявка (опровержение C1 → P3, B-01, O7, O8, D-01, O1, «201/201 OK», «125/125», «18/18»,
«153 PNG», LOC-состав оверлея) не осталась без воспроизводимого артефакта: каждая подтверждена в этом
прогоне либо прогоном закреплённого инструмента, либо независимым пересчётом с перевернувшимся контролем.
Ближе всего к fail — два **числа внутри** P1-цепочек (`0/370` вместо `3/370`; «90 без пола» вместо 89),
но сами P1-выводы (D-01, B-01) воспроизведены, поэтому вердикт — **pass с обязательной эрратой**
в этих двух цифрах, а также в `§5:65` (28→27), `§5:68` (7 из 8→7 из 10), `§5:69` («добавлен»→«изменён»),
`§7:86` (три→четыре «номера шага»), `§2:28` (9→3) и битой ссылке `§4:55` (`LevelObstacles.swift:431-478`).
