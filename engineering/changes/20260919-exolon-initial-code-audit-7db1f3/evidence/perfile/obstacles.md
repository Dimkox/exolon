# Пер-файл аудит: `Exolon/GameCore/Objects/LevelObstacles.swift` (880 LOC, HEAD 52795d1)

Лейн: только этот файл + то, как его классы instantiate-ятся (`TMXLevelRuntime.swift`) и убиваются/летализируются (`GameScene.swift`). Продуктовое дерево не трогалось; все расчёты геометрии — реплика Swift-формулы в `/tmp/perfile-obstacles/analyze{,2,3,4}.py` на 125 TMX из `Exolon/Resources/`.

## Итог

1. **O8 (бонус гасит пусковую) — подтверждён, механизм ровно такой, как описано**, и он хуже, чем «зона 006»: 16 из 19 пусковых задеваются корпусом стоя на полу (10 стоят точно на поверхности пола, ещё 6 приподняты на 32 px), поэтому сбор бонуса (и мгновенная смерть пушки) происходит на обязательном маршруте в **16 картах**. Ни бластер, ни граната пусковую не убивают — она отсутствует во всех списках `destructible*`, так что состояние `isActive=false` необратимо до пересборки зоны.
2. **Новый, более тяжёлый класс дефектов, чем O8: obstacle-геометрия строится от сырого `y` TMX-объекта без сверки с поверхностью, по которой идёт Vitorc.** Проверено на всех 125 картах:
   * **43/46 поршней** лежат на 64/160/272 px **ниже** ходовой поверхности: полностью выпущенный поршень не поднимается над полом вообще; у 40 из них верх хитбока касается поверхности пола с нулевой площадью, т.е. летальность держится исключительно на правиле «rects that touch at an edge intersect» (замерено: `CGRect.intersects` → `true` на кромке), а 3 поршня не летальны ни при каких semantics. Корректны ровно 3 поршня: `L01S03` и два из `L01S10` — т.е. ровно те, что README чинит формулировкой «Zone 009 pistons now use … y=320 (not y=384)». Фикс применили к одной карте.
   * **18/37 gun machine** выплёвывают снаряд **внутри** solid-полосы (`insideSolid=True` на 100% из 18) → снаряд умирает в первом же `updateEnemyBullets`, башня играет анимацию отдачи и не наносит урона никогда; её `hitbox` при этом тоже заканчивается ровно на поверхности пола.
   * **15/37 gun machine** дают выстрел на `feet+40` → снаряд попадает и в стоячего (63 px), и в пригнувшегося (49 px damage-box) → «пригнуться от пушки» (ровно то, ради чего написан `GameConstants.swift:24-31`) не работает. Корректно ведут себя **4/37** башни (`L01S01`, `L01S03`, `L01S08`×2).
   * **3 из 19 двойных пусковых** недостижимы или висят в вакууме: `L01S14` x=432 y=64 при полу 160 и `L04S13` x=432 y=256 при полу 96 (прыжок даёт максимум +37.8 px → разрыв 96/160 px не покрывается; бонус нельзя собрать), `L03S18` x=432 y=224 — под ним вообще нет collision-плиток.
3. **O2 — подтверждён, но локация цитаты описывает следствие, а не причину**: `TMXLevelRuntime.swift:264-269` действительно превращает `blk_tower_rocket` в статичный `DestructibleObstacle`, однако истинная причина в моём файле — **типа «ракетная башня» здесь не существует вообще**: нет класса, нет `EnemyTurretBullet.Kind.rocket`, нет порога «дальше 30 юнитов», нет скорости −2 px/tick, нет 50 очков за сбитие, нет rocket/player collision. Раздел `ORIGINAL_MECHANICS.md:52-57` не реализован ни в одном месте. 29 объектов `rocket` (23 с `blk_tower_rocket`) — декорации.
4. **O9 — подтверждён арифметически: 24 `blk_waggon` + 9 `blk_mushroom` = ровно 33 маркера**, и ни один из них не имеет ветки в `buildObjectsFromTMX`, т.е. объект не создаётся — уничтожать нечего. Шире: **72 из 127 `source_marker` (57%) не порождают ничего** (ещё `blk_gunMachine_BOTTOM` 18, `blk_blinker` 19, `blk_topdown_electro` 2).
5. **O4 — подтверждён и усилен**: файл реально спавнит **одного** летающего врага (пузырь) с **одной** траекторией из трёх закодированных; в данных `behavior` имеет только значение `swing` (28) либо отсутствует (3 → дефолт `swing`), т.е. ветки `circular`/`zig_zag` — мёртвый код. Sprite family (`Int.random(in: 0..<6)` на каждый пузырь) рандомится, а не берётся из таблицы; лимита «6 активных слотов» нет (13 карт с 2 креаторами ≈ 11 пузырей одновременно); гейт `player.position.x <= 320` не совпадает с документным `X >= 84` ни при одном из двух правдоподобных скейлов (×4 → 336; 160→512 → 269).
6. **Пистоны/мины: шлюз летальности в этом файле не реализован вообще** — он целиком в `GameScene.swift:545-566`, и он **не консистентен по направлениям**: с включённым костюком мина не просто не убивает — она не детектируется и не переходит в spent (оригинал: `KillPlayer_unless_Exoskeleton` = сработала, но не убила), при этом в посмертной защите (`invulnerability > 0`) мина всё же разряжается; мина проверяется по `movementHitbox`, поршень — по `damageHitbox`; поршень/мина не участвуют в `solidRects`, поэтому со включённым костюком можно встать внутрь выпущенного поршня и получить мгновенную смерть в момент выключения костюка.
7. Crash-риска в файле нет: ни force-unwrap, ни `try!`, ни индексов за границами (`min(7, …)` в `:90`, `% frames.count` в `:305,:502,:665`), `while`-циклы ограничены `maximumFrameTime`. Все 15 имён текстур существуют в Resources-фазе, и **фактические размеры листов совпадают с дробными `SKTexture(rect:)`-слайсами** (bubble 96×192 = 3×6×32, teleport 512×96 = 8×64, turret_tube 256×16 = 8×32, egg 32×16 = 2×16, missile 64×32 = 2×32) — это позитивный результат. Повреждений за один шаг не удваивается (`hitPlayer()` ставит `isDying`, каждая ветка делает `return`).

**Приоритет на исправление:** сначала OBST-01/02/03/04 (молча сломанные механики на 18–33 картах), затем OBST-05 (отсутствующий тип), OBST-06/07/08.

## Таблица находок

| ID | Pri | file:line | код | триггер | Linux/macOS |
|---|---|---|---|---|---|
| OBST-01 | P1 | `LevelObstacles.swift:685,699,718-722` + `TMXLevelRuntime.swift:219-223` + `GameScene.swift:299` | `isActive = false` / `node.alpha = 0.45`; стреляющий путь: `guard isActive, !player.isDying` | пересечение `player.movementHitbox` с видимым хитбоксом 64×48 пусковой (16/19 задеваются стоя на полу → неизбежно на маршруте); вызывается и во время анимации смерти | Linux (код+данные); macOS — подтвердить «призрак 45%» и 2000 очков на L04S13 |
| OBST-02 | P1 | `LevelObstacles.swift:327-330,341-345` + `TMXLevelRuntime.swift:286-289` | `hiddenY = groundY - 64`, `exposedY = groundY`, `hitbox = CGRect(y: groundY, height: visibleHeight)` | любой поршень из 43 «off-surface» карт (25 карт): базовый y = T−64 → выпущенный поршень не поднимается над полом, летальность = 0-площадной edge-contact (или её отсутствие), 3 поршня не летальны совсем | Linux (пересчёт 125 TMX); macOS — один кадр: встать на поршень в `L01S15`/`L02S07` и переждать цикл |
| OBST-03 | P1 | `LevelObstacles.swift:78-83` (`muzzle = CGPoint(x: hitbox.minX + 2, y: hitbox.minY + 56)`) | выстрел на `feet+40` при базе башни на 16 px ниже пола | 15 башен (L01S13, L01S17, L01S20, L02S05, L02S06, L02S15, L02S17, L02S23, L03S23, L04S19, L05S05, L05S06, L05S15, L05S17, L05S23): приседание не спасает против `GameConstants.swift:24-31` | Linux (арифметика боксов); macOS — присесть под выстрелом в L01S13 |
| OBST-04 | P1 | `LevelObstacles.swift:78-83` + `GameScene.swift:415` | та же формула при базе на 80 px ниже пола → ядро 4×4 спавнится внутри solid | 18 башен (L01S24, L02S08, L02S21, L02S22×2, L03S04, L03S08, L03S12, L03S22, L03S24, L04S04, L04S07, L04S23×2, L05S08, L05S21, L05S22×2): `insideSolid=True`, снаряд уничтожается на первом шаге → башня декоративна | Linux; macOS — визуально: отдача есть, пуль нет |
| OBST-05 | P1 | `TMXLevelRuntime.swift:264-269` (следствие) / отсутствие типа в `LevelObstacles.swift:115-179` | `case "rocket": addDestructible(name: "rocket", …)`; `enum Kind { case turret, doubleLauncher }` | карта с `blk_tower_rocket` (23 объекта / 15 карт) — башня стоит и не стреляет никогда | Linux (код+данные); macOS — зона `L01S07`/`L01S22` |
| OBST-06 | P1 | `TMXLevelRuntime.swift:356-360` + `LevelObstacles.swift:766,778-789` | `if source.contains("beam_") { … CGRect(x: sx, y: max(0, bottomY - 240), width: 48, height: 272) }`, `private var hitPoints = 25` | 10 карт с парой up/down: направление игнорируется, объединение боксов = `0..384` → луч нельзя ни перепрыгнуть, ни пригнуться; нужно 50 попаданий вместо 25 | Linux (пересчёт 10 карт); macOS — тайминг/ощутимость 50 хитов, чёрная дыра 48×272 после сноса |
| OBST-07 | P2 | `LevelObstacles.swift:746-756` (корень API) + `GameScene.swift:545-553` | `isArmed = false; node.removeFromParent(); return true` — вход, расход и урон в одном bool; выше: `if player.hasExoskeleton { continue }` | включить костюк и пройти минное поле: 53 мины остаются взведёнными; выключить костюк стоя на мине → смерть; в посмертной защите мина разряжается без урона (расхождение двух «щитов») | Linux (код+данные); macOS — L01S17 (мины зоны 016; первый changing room — `L01S10`, зона 009) |
| OBST-08 | P2 | `LevelObstacles.swift:414,419-424,449,487-500` | `guard player.position.x <= 320`; `Int.random(in: 0..<6)`; `Double.random(in: 1...3) * sin(position.x/20)`; ветки `circular`/`zig_zag` | любой креатор: 28/31 = `swing`, 3 без свойства (дефолт `swing`) → 100% данных идёт по одной не-табличной ветке; лимита 6 слотов нет | Linux (код+данные); macOS — только субъективная близость траектории |
| OBST-09 | P2 | `TMXLevelRuntime.swift:351-408` (нет ветки) | `blk_waggon`(24) + `blk_mushroom`(9) = 33 → `default:`-подветка «intentionally ignored» | карта из 20 с waggon/mushroom на маршруте: блок нельзя ни подорвать, ни пройти как разрушаемый | Linux (только данные); macOS — L01S23/L02S01 |
| OBST-10 | P2 | `TMXLevelRuntime.swift:28` + `GameScene.swift:574-578` | `private(set) var sourceHazards: [CGRect] = []` — `.append` в дереве отсутствует | мёртвый канал урона: HV (`blk_topdown_electro` 2) и blinker (19) объявлены «не уроном», проверка не срабатывает никогда | Linux (grep + код) |
| OBST-11 | P2 | `LevelObstacles.swift:699-704` + `GameScene.swift:299` | `let distance = hitbox.minX - player.movementHitbox.minX; guard distance > 110` | игрок правее пусковой (телепорт в L01S15 x=368 → выход на 392 при пусковой 288) → знаковое расстояние отрицательное, пусковая молчит | Linux; macOS — подтверждение маршрута |
| OBST-12 | P3 | `LevelObstacles.swift:4-26` | `final class CocoonObstacle` — 0 использований в дереве | мёртвый дубль кокона (спавнится как `DestructibleObstacle`, `TMXLevelRuntime.swift:250-256`) | Linux (grep) |
| OBST-13 | P3 | `TMXLevelRuntime.swift:158` + `LevelObstacles.swift:620-660` | `egg.fixedUpdate(dt: terrainRects: terrainRects)` — computed property, пересобирает массив (плюс два `subtract`-flatMap) на каждое яйцо каждый шаг | 16 яиц (2 инкубатора) × ~12 rect + путь пуль `GameScene.swift:415` | Linux (под счёт 60 Hz); macOS — только если нужны профили |
| OBST-14 | P3 | `LevelObstacles.swift:102,331,383,419,422,449,493,578,580,671,672,686,708,713` | `Int.random`/`Double.random`/`Bool.random` без сида | любой прогон невоспроизводим: нельзя доказать «пузырь убил»/«поршень не убил» повтором | Linux (код) |
| OBST-15 | P3 | `GameScene.swift:580-597` (очки-источники: `LevelObstacles.swift:432,562`) | контакт пузыря/яйца с игроком даёт `awardPoints(bubble.points/egg.points)` | смерть об объект = +150/+50, в TEST INVULNERABILITY — ферма очков; документ привязывает 150/50 к «Blaster kill» | Linux (код+док) |
| OBST-16 | P3 | `LevelObstacles.swift:633-645` | `terrainRects.first(where: { currentBox.intersects($0) })` → `position = previous` | отскок по оси «первого, а не ближайшего» rect + откат по обеим осям; яйцо, спавнувшееся в стене, уходит в вечный откат | Linux (проверено: ни одна камера инкубатора не пересекает collision → не проявляется); macOS — не нужно |

## Подтверждение/опровержение чужих claims

### Подтверждено (с уточнением механизма)

* **O8 (`exolon-full-audit-20260920.md:55`, цитата `LevelObstacles.swift:699,718-723,431-478`) — ПОДТВЕРЖДЁН, механизм ровно тот.** `isActive` пусковой меняется только в `collectBonusIfTouched` (:718-722); `fixedUpdate` отсекается тем же `isActive` (:699); пусковой нет ни в blaster-списке (`GameScene.swift:407-413`: turrets/destructibles/incubators), ни в grenade-списке (`:437-456`), ни в `solidRects` ⇒ ни одно оружие не может ни убить, ни «починить» её. Счётчики совпали: **19 объектов / 18 карт** (`L01S07, L01S11, L01S14, L01S15, L01S21, L02S04, L02S09, L02S14, L02S24, L03S03, L03S18, L04S03, L04S13, L04S21, L05S04, L05S09, L05S14, L05S24`), из них **L04S13 содержит 2** → там 2000 очков и две мёртвые пушки. **Усиление:** «редкий edge case» линеен — 16 пусковых собираются стоя на полу (`L01S07` x=288 y=64, `L01S11` 368/96, `L01S15` 288/192, `L01S21` 432/240, `L02S04` 256/64, `L02S09` 432/240, `L02S14` 432/256, `L02S24` 448/80, `L03S03` 384/64, `L04S03` 336/96, `L04S13` 416/96, `L04S21` 432/96, `L05S04` 256/64, `L05S09` 432/240, `L05S14` 432/256, `L05S24` 448/80). Плюс отсыл к первоисточнику: `ORIGINAL_MECHANICS.md:51` требует **невидимую** бонус-область, а реализация использует видимый `hitbox` всего 64×48 — то есть «бонус» забирается, войдя корпусом в ствол.
* **O2 (`exolon-initial-audit.md:341`, локация `TMXLevelRuntime.swift:264-269`) — локация верна как место, где ракета становится статичным пропа; причина глубже.** `rocket.png` 64×96 существует и в Resources, но `LevelObstacles.swift` не содержит ни класса башни, ни `Kind.rocket`, ни порога дистанции, ни скорости −2 px/tick, ни 50 очков, ни коллизии rocket/player (всё это требует `ORIGINAL_MECHANICS.md:52-57`). Дополнительно: реальные «пушечные» маркеры оригинала представлены в данных двумя ветками, и **`blk_gunMachine_BOTTOM` (18 маркеров / 15 карт) не имеет обработчика вовсе** (`:351-408`), поэтому «комбинированная верхняя/нижняя пушка» walkthrough-зоны 023 существует только в половинном виде (это O1 чужого лейна, здесь — как подтверждение, что «ракетные башни» не «не стреляют», а «не портированы»).
* **O4 (`exolon-initial-audit.md:342`, «Swing-пузыри вместо таблиц врагов») — ПОДТВЕРЖДЁН и усилен** (см. п.5 Итога и OBST-08). Перечень того, что файл реально спавнит: `BubbleEnemy` (один спрайт-семейственный лист `bubble.png`, 6 цветовых рядов, выбираемых случайно) и `EggEnemy` (8 штук на инкубатор, всего ≤16 на карту); динамически спавнятся только пузыри и `HomingMissile`, всё остальное — из `buildObjectsFromTMX`. Таблицы `tab_enemy`/`actions_enemy_trajectory.asm` не имеют ни одного представления в коде: нет ни индекса таблицы, ни sprite family, ни spawn-delay из данных (все 31 креатор имеют `delay=1.0` или не имеют его).
* **O9 (`exolon-initial-audit.md:345`, «33 маркера») — ПОДТВЕРЖДЁН арифметически: 24 + 9 = 33.** Правило уничтожаемости в коде — не таблица, а «есть ли у объекта `destroy()`/`collect()`/`hitByBlaster()`, вызываемый из `GameScene`»: см. матрицу ниже. `blk_waggon`/`blk_mushroom` не создают объект, поэтому они не «не уничтожаемы», а «не существуют», что для play-теста одно и то же, а для починки — разные работы.
* **O7 (`exolon-initial-audit.md:338`, «два 25-hit поля ⇒ 50 хитов») — ПОДТВЕРЖДЁН, и уточнён:** геометрия не «два поля», а одна непрерывная летальная колонка `0..384` в 48-px окне: `up` = `112..384`, `down` = `0..272`, пересечение `32×160` на всех 10 картах (`L02S11 L02S15 L02S17 L03S01 L03S13 L03S19 L04S18 L05S11 L05S15 L05S17`). Walkthrough-пункт «Zone 035: 25-hit force field» = `L02S11`, где нужно 50. Из-за `first(where:)` (`GameScene.swift:399`) урон всегда идёт полю с индексом 0, поэтому второе поле начинает получать урон только после сноса первого; на `L02S15`/`L05S15` (пол 64) стоя и присев попадают **только** в `down`, а `up` требует выстрела с `y ≥ 112`, т.е. из прыжка (прыжок = 37.8 px, ноги до ~102, выстрел ~135 — проходит). Hard-lock не возник, но цена — 50 хитов с обязательной сменой позы.

### Опровергнуто / требует поправки

* **`gamescene.md:170,173` («pistons are lethal when exposed» и «protects against mines/pistons» — «выполнено»).** По шлюзу кода — верно (`:556-566` + `.zero` при скрытом поршне). По геометрии — **неверно для 43/46 поршней** (OBST-02): шлюз висит на `box.width > 0`, а `visibleHeight` считается от собственного `groundY` поршня, а не от поверхности пола, поэтому при базе `T−64` «выставленный» поршень имеет нулевую высоту над полом. README-утверждение выполняется ровно на `L01S10` (зона 009) и `L01S03`.
* **`exolon-full-audit-20260920.md:16` про «56 rocket launchers» — согласен с опровержением**, и добавляю: в данных `rocket`-объектов не 56, а **29 (15 карт)**, из них 23 несут `sourceBlock=blk_tower_rocket`; `turret` — 37 объектов (15×`blk_gunMachine1`, 18×`blk_gunMachine_TOP`, 4 без `sourceBlock`).
* **Проверено и НЕ является дефектом (снято с собственной гипотезы):**
  * Камеры инкубатора (`+16/+48`, 32×32) ни на одной из 22 карт не пересекают collision-слой → яйца не застревают в стене (OBST-16 остаётся как латентная хрупкость кода).
  * `capsule`/`blk_changing_room`: `y: max(0, trigger.minY - 16)` съедает ровно полосу **над** опорной плитой (для `L02S10` исключение = `240..368` при верхе плиты 240) → пол под кабиной сохраняется; комментарий «start 16 px above the supporting platform» соответствует факту.
  * `TeleportPortal.fullyContains`: для flush-портала окно выравнивания 18 px по X и 33 px по Y, стойка 46×63 и присед 46×52 в него помещаются, `interactionRect` по центру не противоречит → требование «целиком внутри» выполнимо (все 70 порталов образуют пары: `portals.count ∈ {0, 2}`, `teleportDestination` корректен).
  * capacity: документные «24 сферы» не нарушаются — максимум **16** яиц на карту (2 инкубатора).
  * Текстур: все 15 имён, которые читает этот файл, лежат в Resources-фазе, размеры листов совпадают со слайсами; `SKTexture(imageNamed:)` не может дать nil-краш.
  * Двойной урон за шаг: невозможен (после `hitPlayer()` стоит `guard !player.isDying, invulnerability <= 0` внутри `hitPlayer` + `return` в каждой ветке).
  * `v1`-аудит про O2/O4/O9 статусов не менял («недоделки оригинала, не регрессии README») — согласен, кроме O8, который в этом отчёте явно переведён в «усилен» и подтверждён.

## Матрица уничтожаемости объектов

Правило в коде единственное: объект умирает, только если `GameScene` вызывает его `destroy()`/`collect()`/`hitByBlaster()`. Таблицы уничтожаемости (`DestroyableBlockSizeTable` упомянута в `TMXLevelRuntime.swift:395`, но не реализована) нет.

| Объект (TMX name / sourceBlock) | создаётся | бластер | граната | контакт игрока | очки | объектов в 125 TMX | итог |
|---|---|---|---|---|---|---|---|
| `turret` / `blk_gunMachine1`,`_TOP` | `TMXLevelRuntime.swift:245` | нет (поглощается, `:407`) | **да** (`:437`) | нет (solid) | 150 | 37 | жив, но 18 молчат, 15 не уклоняемы |
| `cocoon`,`radar`,`rocket`,`gate` / `blk_tower_dish`,`blk_tower_rocket`,`blk_gate_green` | `:250,257,264,335` (`DestructibleObstacle`) | нет | **да** (`:444-448`) | нет (solid) | 150 | 1+12+29+6 | статичны; `rocket` должен стрелять (OBST-05) |
| `incubator` / `blk_birthpod` | `:313` | нет | **да** + освобождает 8 яиц (`:451`) | нет (solid) | 150 | 22 | OK |
| `EggEnemy` (из инкубатора) | `LevelObstacles.swift:558` | **да** (50, `GameScene:391`) | нет | да: умирает +очки +смерть (`:590`) | 50 | 8×22=176 | OK кроме OBST-15 |
| `bubble_creator` → `BubbleEnemy` | `:308` → `LevelObstacles.swift:428` | **да** (150) | нет | да: +очки +смерть (`:580`) | 150 | 31 креатор | O4, OBST-08 |
| `double_launcher` / `blk_double_barrel` | `:322` | нет | нет | «сбор бонуса» (`:718`) | 1000 | 19 | **не убивается ничем, гасится бонусом** (OBST-01) |
| `mine` / `blk_mine` | `:327` | нет | нет | да → расход (`:746`) | 0 | 53 | уничтожить нельзя; с костюком не расходуется (OBST-07) |
| `piston` / `blk_anim_pump` | `:286` | нет | нет | да, если прошёл шлюз | 0 | 46 | не убиваем (по документу не требуется); 43 с битой геометрией (OBST-02) |
| `source_marker` + `beam_up`/`beam_down` → `ForceFieldBarrier` | `:356-360` | **да**, 25 попаданий (`:766,778`) | нет (по документу и верно) | смерть (`GameScene:568`) | 1000 | 20 (10 карт) | 2 поля = 50 (OBST-06) |
| `source_marker` + `control_beacon` → `GreenMissileGuidance` | `:394-407` | нет | **да** (`:458`) | — | 150(+850) | 13 | OK |
| `HomingMissile` | `TMXLevelRuntime.swift:169-173` | нет (по документу) | нет | смерть (`GameScene.swift:539`) | 0 | din. | OK |
| `teleport` / `blk_teleportGate` | `:274` | нет | нет | нет | 0 | 70 | OK (не должен умирать) |
| `ammo_pack`,`grenade_pack` / `blk_box_white`,`_yellow` | `:271,276` | — | — | сбор → 99 / 10 | 0 | 48+38 | OK |
| `capsule`, `blk_changing_room`, `blk_stage_end`, `blk_beacon_base` | `:294`, `:369`, `:371`, `:380` | триггеры/исключения, не объекты | — | — | — | 1+4, 5, 13 | OK |
| **`blk_waggon`** | — | — | — | — | — | **24** | **объект не создаётся** (O9) |
| **`blk_mushroom`** | — | — | — | — | — | **9** | **объект не создаётся** (O9) |
| **`blk_gunMachine_BOTTOM`** | — | — | — | — | — | **18** | объект не создаётся (O1) |
| **`blk_blinker`** | `:366` `break` | — | — | — | — | **19** | намеренно нет урона; реализации мигания нет |
| **`blk_topdown_electro`** | `:361` `break` | — | — | — | — | **2** | HV отсутствует; `sourceHazards` пуст (OBST-10) |
| `CocoonObstacle` (класс в файле) | `LevelObstacles.swift:4-26` | — | — | — | — | 0 | мёртвый код (OBST-12) |

Итого по `source_marker`: **55 из 127 порождают объект/исключение, 72 (57%) — молча исчезают.**

## Что проверить на macOS

Порядок по цене/ценности; всё ниже — play-тест, статикой не доказывается «ощущение», но уже доказано «что код посчитал»:

1. **Поршень в `L01S15` (x=128/240/352, база y=0) или `L02S07` (x=240/352/464, база 16):** встать ногами на пол точно над поршнем и переждать полный цикл. Ожидаемое по README/ORIGINAL_MECHANICS: поршень поднимается в игровую область и убивает. Фактически (по расчёту) он не поднимается над полом; вопрос только в том, сработает ли 0-площадочный edge-contact. Если смерти нет — OBST-02=P1; если смерть есть ровно в фазе `exposed` (1.0 s) — OBST-02=P1 с формулировкой «невидимая напольная мина вместо кранша».
2. **`L01S10` (зона 009, поршни x=64/192):** тот же тест как контрольный случай — здесь поршень действительно поднимается над полом (база == поверхности). Контроль обязан перевернуться относительно п.1.
3. **Башня в `L03S04` (x=208, база 160 при полу 240) и `L03S08` (x=416, база 80 при полу 160):** отдача проигрывается, снаряда в поле нет (умирает в `terrain`) — 18 карт.
4. **Башня в `L01S13` (x=416, база 48 при полу 64):** присесть под выстрелом. Ожидаемое (по `GameConstants.swift:24-31`) — пуля проходит над головой; фактическое ожидание по расчёту — попадание в `feet+40`. 15 карт.
5. **`L01S07` (зона 006) и `L04S13`:** дойти до пусковой, собрать +1000, затем (a) убедиться, что она навсегда перестала стрелять и осталась висеть полупрозрачным «призраком», (b) на L04S13 получить 2000 за две пусковые. Это же проверить в TEST INVULNERABILITY и во время анимации смерти (падение трупа через пусковой так же забирает бонус).
6. **`L02S11` (зона 035):** засечь число выстрелов до пропажи луча (ожидание 50 вместо документных 25) и осмотреть чёрный прямоугольник 48×272, который затирает art после сноса поля (`:784-785`, z=6).
7. **`L01S17`/`L02S02` с включённым костюком** (первый changing room — `L01S10`, зона 009, так что костюк к этим минам уже есть): пройти по минам (они не должны расходиться) и по включённому поршню, затем выключить костюк внутри поршня — мгновенная смерть без какого-либо предупреждающего состояния.
8. **`L01S14` (x=432 y=64 при полу 160) и `L04S13` (x=432 y=256 при полу 96):** убедиться, что бонус недостижим (нужен подъём на 96/160 px, прыжок даёт 37.8), т.е. +1000 и «смерть пушки» там просто не существуют; плюс `L03S18` (x=432 y=224) — пусковая висит над провалом без collision-плиток.
9. **`L01S21`/`L02S06`/`L02S10` (2 креатора на карту):** посчитать одновременно живых пузырей (ожидание по документу ≤6, по коду без лимита ≈11) и заметить, что цвет каждого пузыря случайно-разный.
10. **Звук/перф не в лейне**, но п.13 (`terrainRects` на каждое яйцо) стоит проверить в Instruments только если появятся другие признаки 60 Hz-просадок — по объёму данных (avg 12.5 rect) он не критичен.

Вердикт: fail
