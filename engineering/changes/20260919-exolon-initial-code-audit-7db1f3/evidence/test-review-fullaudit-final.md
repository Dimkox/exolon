# Test review — адекватность верификации деливерабла «Полный аудит Exolon» (финальное дерево)

**Роль:** route-selected `test_reviewer` (`.grok/agents/test_reviewer.md`), read-only волна.
**Маршрут:** `7db1f3f0b126` (intent=`review`, `write_agent=null`, `quality_profiles=["base"]`).
**HEAD:** `52795d1` (`codex/factory-updated-20260920`) · **base:** `403eb13` · **tree fingerprint (фактический, вычислен инструментом фабрики):** `8d782be4ceb7af82e745b3f572894b2a3a67f223aea5e26533c56467406123c8`.
**Хост:** Ubuntu 24.04 x86_64. Продактив-код не изменялся; из файлов дерева записан только этот отчёт (см. §8 «гигиена прогона»).
**Вопрос ревью:** достаточна и честна ли доказательная база аудит-документа (а не «хорош ли продукт»), и что именно не покрыто ни одним автоматическим гейтом.

---

## 0. Вердикт

# Вердикт: pass

Вердикт: pass

**с четырьмя обязательными правками (F-01, F-02, F-03, F-04) и одним обязательным процессным шагом (F-11).**

Обоснование «pass», а не «fail»:

1. **Ни одно продуктовое утверждение не выдаётся за проверенное сборки/геймплеем.** Я прошёл все места, где в отчётах стоит «PASS/OK»: `swiftc -frontend -parse 18/18` (в той же строке назван потолок — «тип-чек и сборка невозможны без Apple SDK»), `201 OK` (это тесты factory-оверлея, §5 озаглавлен «Состояние factory-оверлея»), xmllint/pngcheck (данные, не игра). `FORBID-003` («Claim Swift/xcodebuild or gameplay pass on Linux») **не нарушен** — нарушений, за которые ставят fail, нет.
2. **Ключевые измерения воспроизводимы и я их перемерил независимо** — включая опровержение C1 (постройтовый замер + мутационный контроль), разбивку B-01 (35/90), beam-матрицу (10/10), джойн «документ ↔ TMX» (72/145, 5 семейств по 100 %), `solid=` (0/125 при двух разных дефинициях), LOC-метрики (25 413 / 21 071 / 21 105 / 13 368 / 4 227, «≈14×» = 14.16 — сходится), инвариант «продукт не тронут» (`git diff 403eb13 HEAD -- Exolon Exolon.xcodeproj README.md ORIGINAL_MECHANICS.md LEVEL_COMPILER_AUDIT.md` → пусто).
3. **Правки предыдущей test/code-волн реально инкорпорированы**, а не обещаны: 4 кабины с нулевым зазором добавлены в macOS-чеклист (`exolon-full-audit-20260920.md:96` §9.3), «только штатный флоу» снят и возвращены поршни/второй UP (§9.2), «19 объектов на 18 картах» исправлено (я пересчитал: ровно так, `L04S13` содержит 2), LOC-числа исправлены на воспроизводимые, скрипт замеров закоммичен (`evidence/fullaudit_measurements.py`), AC-F-007 закрыт.
4. **Тест-гейп (0 XCTest-файлов у игры) не блокер для review-маршрута** и корректно отслеживается: T3 в §6/§7 + требование «константа + юнит-гейт при первом Swift-таргете» + §9 как ручная замена. Блокировать аудит из-за отсутствия Swift-тестов на Linux — то же самое, что объявлять `parse` за `build`.

Почему не «чистый pass»: доказательная база **поверхностно** честна, но **частично не переносима** (ссылки на удалённые `/tmp`-артефакты, три гейта без закоммиченного инструмента, порождённый `.md`, который не воспроизводится побайтно) и содержит **четыре lossy-сжатия**, где заголовок отчёта сильнее собственного evidence (F-06, F-07, F-08, F-09).

---

## 1. П.1 брифа — критерии приёмки: имя → артефакт (или «артефакта нет»)

Машинно авторитетны typed-критерии `change-spec.yaml`; `requirements.md` — проза (его шапка это и заявляет).

| Критерий | Формулировка | Артефакт, который его доказывает | Моя оценка |
| --- | --- | --- | --- |
| AC-001 | пакет существует, frozen-пути не тронуты | `engineering/changes/…/brief.md` + **пере-мерено мной**: `git diff 403eb13 HEAD -- Exolon Exolon.xcodeproj README.md ORIGINAL_MECHANICS.md LEVEL_COMPILER_AUDIT.md` → пусто | **доказано** |
| AC-002 | три аналитика написали отчёты | `evidence/analysis-repo_explorer.md`, `analysis-architect.md`, `analysis-docs_researcher.md` (все три в `git ls-files`) | **доказано** (но это v1-отчёты; три `-fullaudit`-отчёта в typed-спеке не поименованы — F-10) |
| AC-003 | Linux-скрипт даёт 125 TMX, pbx-членство, инвентарь объектов, версии | `evidence/linux_static_audit.py` + `linux-static-audit.{md,json}`; **пере-запуск в песочнице**: `summary identical: True`, `zones identical: True` (125), `tmx_on_disk 125`, `swift_files 18`, `marketing ['0.5']`, `info_plist 0.3`, `resource_file_count 282` | **доказано**, кроме `.md` — он не воспроизводится побайтно (F-04) |
| AC-004 | отчёт различает подтверждено/гипотеза/ограничения | `engineering/reports/exolon-initial-audit.md` + корректирующая шапка + `exolon-full-audit-20260920.md` | **доказано** (колонка «Статус» §4 и теги §2 сверены мной выборочно) |
| AC-005 | приоритетизированный локальный бэклог | `engineering/reports/exolon-initial-audit-backlog.md` | **артефакт есть, но он противоречит авторитетному отчёту**: шапка снимает C1/C1b, а таблица строк 19–20 по-прежнему `P0` + инструкция «Play → если провал, `minY+16`» (F-09) |
| AC-006 | `grok_verify --mode pr` выполнен и записан честно | `.grok-stack/runtime/receipts/7db1f3f0b126/verification.json`: `status: pass`, `mode: pr`, 13 записей проверок (10 выполнено + 3 `skip` = architecture/governance/workflow-artifacts, «not_configured»), `criterion_ids: ["AC-006"]`, `tree_fingerprint = 8d782be4…` = **текущий** | **доказано на финальном дереве** |
| AC-007 | независимые `code_reviewer`/`test_reviewer` **для финального дерева** | `evidence/code-review-fullaudit.md` + `evidence/test-review-fullaudit.md` (тексты есть) и квитанции `code_review.json`/`test_review.json` (обе `pass`, `criterion_ids: ["AC-007"]`) — НО `tree_fingerprint = 7301595d…`, т.е. привязаны к `b8aee42`, а не к `52795d1` | **не доказано на финальном дереве**: валидатор фабрики сам отвечает `code_review: stale after repository changes`, `code_review: spec binding stale` (то же для `test_review`) — команда в §2, строка «receipt validator». Правка = перезаписать квитанции после того, как все документы легли в дерево (F-01) |
| AC-008 | валидация сборки/геймплея остаётся pending | §3 и §9/§11 отчётов; **подтверждено мной**: `find . -name '*.xcscheme'` → пусто, Swift-тестов 0, `xcodebuild` на хосте нет | **доказано** |
| AC-F-001…009 | «Full-audit extension» (реальный объём этого прогона) | **только проза** `requirements.md:36-46`; в `change-spec.yaml` ноль упоминаний `full-audit`/`fullaudit` (проверено `grep -c` → 0), поэтому гейт `change-spec` рапортует `criterion_mapped 8/8, unmapped_ids []` и **критериально слеп** к фактическому деливераблу | **артефакта в typed-виде нет** (F-10) |
| INV-001/AC-F-009 | байты продукта не менялись | команда выше (пусто) + гейт `source-stability` = pass в квитанции | **доказано** |

Итог п.1: 8 из 9 typed-критериев закрыты артефактами; AC-007 формально **не закрыт на текущем дереве** (механически: `spec_fingerprint` включает `git rev-parse HEAD` — `.grok-stack/adaptive_grok/spec.py:656-681`, — поэтому любой коммит после `01:51` их переворачивает), а AC-F-* живут только в Markdown.

---

## 2. П.2 брифа — воспроизводимость цифр: чем именно померено

### 2.1 Класс A: померено закоммиченным инструментом ✅

`evidence/fullaudit_measurements.py` (190 строк, три замера + вбитые контроли). Мой перезапуск **в песочнице** (копия скрипта + symlink `Exolon`, чтобы не трогать дерево репозитория):

```
$ python3 fullaudit_measurements.py        # rc=0
cabin_L01S10: booth_cells_before 5, floor_rects_before 1, booth_cells_after_shipped_exclusion 0,
              floor_rects_after_shipped_exclusion 1, control_mutated_exclusion_floor_rects_whole 0,
              verdict "C1 retracted", ok true
beam_matrix:  maps_with_any_beam_marker 10, maps_with_both_up_and_down 10, ok true
b01_spawn:    tally {exact 35, void 89, 16_above 1}, total 125, ok true
stderr: ALL_MEASUREMENTS_MATCH_REPORT   → RC=0
$ cmp /tmp/fa-repro-.../fullaudit-b01-spawn.json <repo>/evidence/fullaudit-b01-spawn.json
BYTE_IDENTICAL        (125 записей, ни одного расхождения)
```

Значит §10-ое утверждение «rc=0 ⇔ все числа совпадают с отчётом» — **истинно и воспроизводимо**, включая то, что JSON-артефакт в дереве порождён именно этим скриптом.

Моя **незавимая** ре-реализация (не через скрипт, а через свои примитивы + обе ветки Swift-геометрии `TMXLevelRuntime.swift:300-306` и `:372-377`) подтверждает и главную находку, и её остаточный риск:

```
maps with a cabin: ['L01S10','L02S10','L03S11','L04S16','L05S10']
L01S10 [capsule] trigger y=128..208 excl y=112..224 floor_top=96  gap=16  floor_rects_removed=0
L02S10 [marker ] trigger y=256..352 excl y=240..368 floor_top=240 gap=0   floor_rects_removed=0
L03S11 [marker ] trigger y=112..208 excl y=96..224  floor_top=96  gap=0   floor_rects_removed=0
L04S16 [marker ] trigger y=80..176  excl y=64..192  floor_top=64  gap=0   floor_rects_removed=0
L05S10 [marker ] trigger y=256..352 excl y=240..368 floor_top=240 gap=0   floor_rects_removed=0
sweep (двигал нижнюю грань исключения вниз): 112..96 → пол цел; 94..80 → пол срезан
```
→ «C1 опровергнут», «запас 16 px», «4 кабины с нулевым зазором», «faithful-мутация ≤95 px прорезает пол» (§2, §9.3) — **все верны**.

### 2.2 Класс B: названо в отчёте, но инструмента в дереве нет ❌

| Цифра в отчёте | Закоммиченный инструмент? | Что нашлось фактически |
| --- | --- | --- |
| xmllint **125/125** well-formed | **нет** | только утверждение §1.3/§10 и перепроверки в `test-review-fullaudit.md:29`, `code-review-fullaudit.md:47` |
| pngcheck **153 PNG** | **нет** | аналогично (`test-review-fullaudit.md:30`) |
| `swiftc -frontend -parse` **18/18** | **нет** | аналогично (`test-review-fullaudit.md:31`) |
| джойн **970 / 72 / 145 / 0-125 `solid=`** | **нет** | `analysis-docs_researcher-fullaudit.md:166` и `:180` — это **заглушки** `$ python3 - <<'PY'  # см. вывод ниже …`, тела нет; полные скрипты объявлены в `/tmp/docsaudit/*`, которого **уже нет** (`ls /tmp/docsaudit` → No such file) |
| **201 тест / 1 ERROR под загрузкой / 251·173·163 s** | **нет** | §10 строка 111 ссылается на `/tmp/exolon-factory-exit{,2,3}.log`; `ls /tmp/exolon-factory-exit*.log` → **No such file**. Квитанция `verification.json` stdout не хранит (`details` для `factory-postgres-exit` = `[]`) |
| «все **27** имён `SKTexture(imageNamed:)` резолвятся» (§3/V3) | **нет** | моя метрика: 23 вхождения `imageNamed`, 23 уникальных литерала, **0** нерезолвимых (вывод V3 не меняется), но единица «27» неоткуда не восстанавливается |

Все эти числа я **всё равно перемерил и они верны** (см. §3). Претензия не в достоверности, а в переносимости: через неделю после закрытия сессии подтвердить их сможет только тот, кто заново напишет измеритель.

### 2.3 Пункр «Swift parse как build/typecheck» — где граница

Граница соблюдена везде, кроме одной формулировочной: §1.2 «**Продукт синтаксически здоров**: … 18/18 PASS» — корректно («синтаксически»); AC-F-003 в `requirements.md:41` называет это «Swift product gate upgraded from "none" to `swiftc -frontend -parse`» — тоже корректно. Слова «build», «compile», «typecheck», «собирается» применительно к продукту в отчётах не встречаются (поиск по «сборк»/«build» даёт только «невозможны без Apple SDK», «не xcodebuild»). **Замечаний нет.**

---

## 3. П.2 брифа — есть ли у каждого пробивателя контроль, показанно способный перевернуться

| Пробиватель | Контроль | Закоммичен? | Мой результат |
| --- | --- | --- | --- |
| `measure_cabin` (C1) | мутация `(352,112,96,112) → (352,95,96,129)`; `ok` требует `control == 0` целых напольных прямоугольников (`fullaudit_measurements.py:104-118`) | ✅ | сработал: control=0, при этом `ok=true`; мой независимый sweep даёт точку перегиба 96/94 |
| токен-сплит CSV | `assert len(gids) == W*H` (`:57`) —implicit-контроль против того самого off-by-one, который и породил ложный C1 | ✅ | 840 = 35×24 для `L01S10` ✔ |
| `measure_beams` | **нет мутации**: только константы `10/10` (`:151`) | ✅ как tripwire | воспроизведено (10/10, список совпадает с §9.6) |
| `measure_b01` | **нет мутации**: `total==125 and exact==35` (`:171`) | ✅ как tripwire | воспроизведено |
| xmllint | «битый XML обязан упасть» | ❌ только в прозе | обрезок 3000 B → `rc=1` ✅; **но**: «headless»-обрезок (снять 39 байт пролога) → `rc=0`, т.е. не каждый «битый вход» падает — контроль надо собирать, а не объявлять |
| pngcheck | «побитый PNG обязан упасть» | ❌ | побитый байт в середине файла → `rc=2` ✅ |
| swiftc parse | «сломанный Swift обязан упасть» | ❌ | `func g(: Int)` → `rc=1` ✅ |
| джойн «документ ↔ TMX» | заявлен как «контроль **0/370** на нерелевантном индексе» (§1.5, строка 16) | ❌ | **не нулевой**: собственный evidence (`analysis-docs_researcher-fullaudit.md:159`) пишет «370 из 370 (**кроме 3**)», а в детализации — `blk_anim_pump {(10,): 2}` и `blk_changing_room {(12,): 1}`. Мой вариант на 410 объектах без `sourceX/Y`: 4 совпадения (3 `piston`, 1 `capsule`). То есть среди «контрольных» есть **осмысленное** совпадение (капсула ↔ тип 12), которое как раз поддерживает одну строку первичной «1:1»-таблицы — см. F-07 |
| `solid=` | «не воспроизводится ни при одной дефиниции (0/125)» | ❌ | ✅ подтверждаю двумя дефинициями: сольид-клетки 35-й сетки → 0/125; число слитых прямоугольников → 0/125 |
| «factory 201, флейк 1/3» | падающий кейс = тот же тест в прогоне №1 | ❌ (логи удалены) | локально: `Ran 201 tests … OK (skipped=71)` — see §4 |

Отдельно о дисциплине: §2 отчёта **сам** задокументировал, что «числа мутационного контроля у архитектора (−32→1, −48→2)» были порождены дефектом токенизатора. Но в закоммиченном evidence (`analysis-architect-fullaudit.md:126-128`) этот неверный блок **остался без внутренного эратума** и геометрически невозможен: при `off=32` коробка исключения = `x=352..448`, а упомянутый там прямоугольник `x=0..288, y=224..240` в `L01S10` отсутствует (в `L01S10` нет ни одного rect с `y=224`; rect `x=0..288` стоит на `y=48..64`). Проверено моим замером. Исторические evidence править не надо (это правило маршрута), но файл, на который §10 ссылается как на доказательство, обязан нести строку «※ эти числа отозваны, см. `fullaudit_measurements.py`» (F-12).

---

## 4. П.4 брифа — что я реально выполнил (дешёвое, уже задокументированное)

```
$ python3 -m unittest discover -s factory/tests -t .
.........................................................................................sssssss…sss.........................................
----------------------------------------------------------------------
Ran 201 tests in 4.875s

OK (skipped=71)
```
→ цифра «201» воспроизводится как **объём discovery**, но не как «201 выполненых»: на этом хосте 71 postgres-тест скипается (`skipUnless(DATABASE_URL…)`). «201/201 OK в чистом контейнере» — про контейнерные прогоны, чьи логи удалены (§2.3, F-02). Полный `grok_verify --mode pr` я **не** перезапускал (он уже выполнен: квитанция `03:07:02`, `pass`, fingerprint = текущий).

Прочие прогоны: `xmllint` (125/0), `pngcheck` (RC=0, 153 файлов), цикл `swiftc -frontend -parse` (18/0) + контроли, `ruff check evidence/*.py` → «All checks passed!», `git diff --check` поведение на «blank line at EOF» (отдельный минимальный git-репозиторий в `/tmp`), песочничные перезапуски `fullaudit_measurements.py` и `linux_static_audit.py`, независимые джойны и sweep. Длительных прогонов не запускал.

---

## 5. П.3 брифа — честность границ (что перегружено)

Сверх F-06/F-07/F-08 (см. §6) — один пункт формулировки, где статический вывод подаётся как наблюдаемое поведение:

* §4 **B-02**: «…`fallbackGroundY` удерживает игрока на выходе карты (в `L05S16` вся коллизия — один rect x=0..368 … — **и всё равно не проваливаешься**). Ямы безвредны **всегда**», колонка «Статус» = «код+данные» (без «play-гипотеза»). Данные я проверил — `L05S16` действительно даёт ровно один прямоугольник `(0, 48, 368, 16)`, а колонки 23…34 пусты полностью; код `Player.swift:291-297` действительно ставит `position.y = fallbackGroundY + h/2` при `position.y <= …`. Но «не проваливаешься» и «безвредны всегда» — суждение о рантайме, которого на Linux проверить нельзя; в §9 плей-пункт про `L05S16` не назван. Достаточно пометки «вывод из кода; play-подтверждение = §9.4».
* Всё остальное помечено корректно: B-03 «код-трассировка; play-гипотеза» (а `GameScene.swift:610-614` действительно вызывает бонус **до** `guard !next.isEmpty` — подтверждено чтением), §9 п.5 и п.8 сформулированы как «ожидается fail»/«ожидаемо ничего не происходит» (и `grep -rn loadCheckpoint --include=*.swift .` → **только определение**, `GameState.swift:40`), B-04 прямо объясняет, почему плей-пункта нет.
* §1.6/§6 («доказательства нигде, кроме рабочего дерева», «неотправленный однофамилец») — **устарело на финальном дереве**: `git ls-files engineering/reports` → три файла (включены в `bcacf30`), `origin/codex/factory-updated-20260920 = 52795d1` (отправлено). См. F-13.

---

## 6. Находки (нумерованы, с северити и доказывающей командой)

**F-01 · Major · process/evidence-binding.** Квитанции `code_review`/`test_review` привязаны к `7301595d…` (HEAD `b8aee42`), т.е. **не** к финальному дереву `52795d1`;typed-критерий AC-007 («reports exist for the final tree») и `requirements.md:46` (AC-F-008) машинно не подтверждены.
Доказывающая команда (read-only, ничего не записывает):
```
$ python3 -c "import sys;sys.path.insert(0,'.grok-stack');from pathlib import Path;\
from adaptive_grok.receipts import validate_evidence;from adaptive_grok.util import tree_fingerprint,load_json;\
r=Path('.').resolve();a=load_json(r/'.grok-stack/runtime/active-route.json');f=tree_fingerprint(r);\
print(f);print(validate_evidence(r,a,current_fingerprint=f))"
8d782be4…  ['code_review: stale after repository changes', 'code_review: spec binding stale',
            'test_review: stale after repository changes', 'test_review: spec binding stale']
```
Механизм: `spec.py:656-681` включает `git rev-parse HEAD` в `spec_fingerprint`. **Важный побочный вывод:** сам акт добавления review-отчётов в дерево инвалидирует и `verification.json` (03:07) — порядок закрытия обязан быть: все документы → `grok_verify` → квитанции ревью → (при изменении дерева) перезапуск. Правка: перезаписать `verification`, `code_review`, `test_review` на финальном дереве; в тексте AC-F-008/§10 указывать явный sha вместо «final tree».

**F-02 · Major · durability.** §10 (строка 111) ссылается на `/tmp/exolon-factory-exit{,2,3}.log` — **файлы удалены**; гейт не сохраняет stdout (`factory-postgres-exit` details `[]`). Значит «3 прогона FAILED-1/OK/OK», «251/173/163 s» и главное — основание upstream **#155** (невоспроизводимый ERROR под загрузкой) не имеют **ни одного** артефакта в дереве. Правка: положить логи (или их хвосты + sha) в `evidence/`, либо переформулировать §1.4/§5 как «наблюдение текущей сессии, артефакт не сохранён».

**F-03 · Major · reproducibility.** Три гейта (§1.3, §1.2: xmllint, pngcheck, `swiftc -frontend -parse`) и джойн §1.5 не порождены ни одним закоммиченным инструментом, а §10 строка 112 отсылает за «командами в §2/§3», где для xmllint/pngcheck команд нет (в §2 — только имена инструментов и результаты), а heredoc'и в docs-evidence — заглушки. Правка: расширить `fullaudit_measurements.py` (или `linux_static_audit.py`) этими четырьмя замерами, либо вклеить точные команды в §10.

**F-04 · Major · self-inconsistency of the documented reproduction.** Побайтная воспроизводимость `.md` сломана: `linux_static_audit.py:549` пишет файл с пустой строкой на конце, а закоммиченный `linux-static-audit.md` (207 строк) урезан коммитом `52795d1`, который существует ровно ради `git-diff-check`. Следствие: читатель, выполнивший §12 первичного отчёта/test-plan:20, **пачкает** трекнутый файл и **роняет** гейт:
```
$ diff <repo>/evidence/linux-static-audit.md <rerun>/linux-static-audit.md
207a208
>
$ git diff --check        # воспроизведено на минимальном git-репозитории
f.md:3: new blank line at EOF.     exit=1  → verification.py:600-641 → git-diff-check = fail
```
(сам JSON и `fullaudit-b01-spawn.json` воспроизводятся побайтно — проблема только `.md`). Правка: `.rstrip()` перед записью `"\n"…` либо `md.pop()`; затем перегенерить артефакт.

**F-05 · Major · coverage.** Ни один автоматический гейт не исполняет ни один закоммиченный измеритель аудита, а линтеры до них не дотягиваются: `verification.json` → `ruff`/`bandit` перечисляют `.grok-stack/adaptive_grok`, `scripts`, `.grok/hooks`, хуки, `factory/src/adaptive_factory` — **без** `engineering/**`; `ruff.toml` явно содержит `exclude = [ … "engineering" … ]`. То есть «rc=0 ⇔ цифры совпадают с отчётом» — ручной обряд; порча скрипта или дрейф данных не видны никому. Правка: либо чек в `grok_verify` (например `audit-measurements`, rc-гейт), либо снять `engineering` из ruff-exclude для `*.py` в `evidence/`.

**F-06 · Minor · measurement ↔ checklist mismatch.** `requirements.md:33` («Linux checks must be rerunnable») — единственное NFR-обязательство — формально выполнено только для JSON. (Относится к F-04; фиксирую отдельно, т.к. правка касается текста NFR.)

**F-07 · Minor → playbook-critical.** §4 B-01 и §9.4 расходятся с закоммиченным JSON. Отчёт: «плоскость «поднимает» или «роняет» игрока на 16 px» и «взять крайние: **16-px подъём vs падение**». Артефакт `evidence/fullaudit-b01-spawn.json`: `exact 35, void 89, 16_above 1`, **16_below — 0** (проверено `Counter`'ом и перезапуском скрипта). Итого: 89/90 случаев — «под ногами вообще нет локального пола», а «падения на 16 px» в дереве не существует; человек на macOS потратит слот чеклиста на вымышленный кейс и не возьмёт доминирующий. Правка: §9.4 → «89 void-карт + 1 карта +16 px (`L03S11`)».

**F-08 · Minor · overstated control.** §1.5 (строка 16): «контроль **0/370** на нерелевантном индексе». Собственный evidence (строка 159 того же пакета): «370 из 370 (**кроме 3**)», причём одно из трёх — `capsule → тип 12`, т.е. осмысленное совпадение, а не шум. Вывод D-01 это не ломает (72 совпадения + 73 нет + `solid=` 0/125 я подтвердил), но «нулевой контроль» в авторитетном отчёте — завышение. Правка: «3/370 (2 `anim_pump`→тип 10, 1 `capsule`→тип 12) — контроль не нулевой, но нерелевантен как индекс».

**F-09 · Minor · lost caveat.** §1.5: «тип 15 = конец стадии, не HV» — в evidence («подтверждённое смещением, а не равенство»: `blk_stage_end {(15, +2, 0): 5}`, перепроверено мной: 5/5) это отдельная, более слабая категория. Авторитетный отчёт, объявивший себя единственным (§шапка), обязан тег «смещение ±2» сохранять.

**F-10 · Minor · typed spec behind the delivery.** `change-spec.yaml` = 8 критериев первичного аудита; объём этого прогона описан только прозой (`requirements.md:36-46`), а гейт `change-spec` при этом зелёный и рапортует `criterion_mapped 8/8, unmapped_ids []` (см. `verification.json → details.spec.coverage`). Смысл: покрытие приёмки считается по устаревшей спеке. Правка: добавить AC-F-* в typed-спеку (или явно пометить их superseding-артефактами).

**F-11 · Major (процесс доставки, вне моей полномочий) · PR-only.** Деливерабл аудита (коммиты `bcacf30`, `52795d1`) лежит в `origin/codex/factory-updated-20260920`, а **открыт** только PR #1 — для другой ветки (`codex/update-factory-20260920`, head `b8aee42`, DRAFT). `gh pr list --state all` → одна строка. То есть ни один документ этого аудита не находится под merge-полномочием, а §6 отчёта описывает ветку как «неотправленный однофамилец» (устарело). Требуется PR на `52795d1` (или перенацелить #1) — решение владельца, не моё.

**F-12 · Minor · evidence annotation.** `analysis-architect-fullaudit.md:126-128` хранит отозванные числа мутационного контроля без пометки; геометрически они невозможны (при `off=32` коробка `x=352..448`, а помянутый rect `x=0..288, y=224..240` в `L01S10` отсутствует; rect `x=0..288` там же на `y=48..64`). Исторический снимок трогать нельзя, но одну строку «※ отозвано, см. `fullaudit_measurements.py`» в конец §Шаг 6 добавить можно без переписывания (или указать, что файл — исторический, прямо в §10).

**F-13 · Minor · stale self-description.** §1.6 и §6 (строки 17, 75): «доказательства первичного аудита живут только в рабочем дереве (untracked)», «оба отчёта `engineering/reports/` … untracked», «8 записей», «ветка … неотправленный однофамилец». На финальном дереве: три отчёта и весь пакет трекаются (`git ls-files`), untracked = 7 записей (`git status --porcelain | grep -c '^??'` —runtime, `__pycache__`, `.qwen/`), ветка отправлена. Побочный, но показательный факт: **импорт/запуск** `fullaudit_measurements.py` создаёт `evidence/__pycache__/` — untracked-шум внутри доказательственного каталога, прямое следствие отсутствующего `.gitignore`, которое отчёт сам же и зафиксировал (свой `__pycache__` я удалил; см. §8).

**F-14 · Info.** `state.json` (переходы `01:46:40`/`01:46:42`) утверждает «grok_verify PASS on the final tree» и «receipts recorded» раньше, чем существуют сами артефакты (квитанции ревью — `01:51:25`, верификация на финальном дереве — `03:07:02`, отчёты закоммичены в `02:01`/`02:08`). Мелочь бумаги, но ровно тот класс рассинхрона, который F-01 делает машинно видимым: связывать надо с sha, а не со словами «final tree».

**F-15 · Info (положительное).** `FORBID-001/002/003`, `INV-001` соблюдены и проверены: продукт побайтно не менялся; внешних тикетов по продукту не заводилось (#155/#157 — вне продукта, в factory, существуют и обоснованы файлами — это подтверждала предыдущая волна, я её выводы не перемерял); заявлений о сборке/геймплее нет.

---

## 7. П.5 брифа — что НЕ покрыто ни одним автоматическим гейтом (остаточный риск)

1. **Весь продуктовый цикл компиляции.** `grok_verify --mode pr` не знает, что такое Swift: маршрутизированный профиль `base`, а `ruff`/`bandit`/`unittest`-команды перечисляют только factory/overlay (см. `verification.json → details.checks[*].command`). Ни `swiftc -frontend -parse`, ни xmllint, ни pngcheck в гейт не входят → регресс в этих трёх замерах не упадёт ни на каком зелёном статусе. (Корень — слепой классификатор, upstream #157: `repo.languages: []` при 18 `.swift` + `.xcodeproj`.)
2. **Ни один поведенческий слой продукта.** 0 XCTest-файлов, 0 интеграционных тестов, 0 characterization-тестов на коллизию/exclusion/`fallbackGroundY`/scoring/persistence. Всё это живёт исключительно в §9 как 11 ручных пунктов на чужом железе.
3. **Верность цифр самого отчёта.** Ни один гейт не сверяет числа в Markdown с деревом; `fullaudit_measurements.py` — единственный самопроверяющийся кусок (rc-гейт), и он (а) не вызывается автоматически (F-05), (б) покрывает 3 из ~15 количественных утверждений (F-03).
4. **Существование цитируемых артефактов.** Гейт не знает, что `/tmp/exolon-factory-exit*.log` больше нет (F-02), и не знает, что heredoc в evidence — заглушка (F-03).
5. **Семантика TMX.** Well-formedness ≠ играбельность: ни один гейт не проверяет, что map-свойства/координаты объектов согласованы с `LEVEL_COMPILER_AUDIT.md` или что `solid=` вообще чем-то скреплён (мной показано 0/125 — и это тоже вне гейта).
6. **Свежесть квитанций ревью как условие «готово».** `grok_verify` прошёл зелёным **при устаревших** `code_review`/`test_review` — то есть зелёная верификация сама по себе не гарантирует, что required_evidence актуальна; это видит только отдельный `validate_evidence` (F-01).
7. **Гигиена, которую отчёт сам зафиксировал:** отсутствие `.gitignore`, ссылки `AGENTS.md` на несуществующие `START_HERE.md`/`PROJECT_STATE.json`/`mistakes.md`/`architecture/`/`trust-ci/`, пустые `engineering/{adr,reviews,contracts/*}`, битые ссылки `factory/README.md`. Ни один из этих фактов не гейтится.

---

## 8. Гигиена прогона (что я трогал)

Продуктовые файлы не изменялись: `git status --porcelain | grep -v '^??'` → пусто (проверено после всех прогонов). Никаких записей квитанций я не делал (`scripts/grok_review.py` имеет только режим записи — не запускал). Все прогоны измерителей выполнялись в песочницах (`/tmp/fa-repro-test-reviewer`, `/tmp/fa-static`) через symlink на `Exolon/`, чтобы не порождать diffs в дереве; исключение — импорт `fullaudit_measurements.py` из каталога evidence, который создал `engineering/changes/…/evidence/__pycache__/`; **удалено мной** (`rm -rf`), состав untracked возвращён к исходному (7 записей). Единственный записанный файл репозитория — этот отчёт.

## 9. Обязательные правки перед закрытием маршрута (сводно)

| Pri | Правка | Файл/строка |
| --- | --- | --- |
| 1 | перезаписать `verification` + `code_review` + `test_review` на финальном sha; в тексте вместо «final tree» указывать sha | F-01, `requirements.md:45-46`, отчёт §10 |
| 2 | вернуть логи 3 прогонов в `evidence/` (или снять ссылку на `/tmp`) | F-02, отчёт §10:111 |
| 3 | закоммитить инструменты для xmllint/pngcheck/swiftc/джойна либо вклеить команды в §10 | F-03, отчёт §10:112 |
| 4 | `.rstrip()` в генераторе `linux-static-audit.md` + перегенерация | F-04, `linux_static_audit.py:549` |
| 5 | wire `fullaudit_measurements.py` в гейт (rc-чек) или снять `engineering` из ruff-exclude | F-05, `ruff.toml` |
| 6 | §9.4 и §4 B-01: «89 void + 1×(+16 px)», убрать «падение на 16 px» | F-07, отчёт:98 |
| 7 | §1.5: «контроль 3/370 (не 0)», вернуть тег «±2 смещение» для типа 15 | F-08/F-09, отчёт:16 |
| 8 | AC-F-* → в typed `change-spec.yaml` | F-10 |
| 9 | PR на `52795d1` | F-11 (владелец) |
| 10 | эратум-строка в architect-evidence; обновить §1.6/§6 (трекинг/отправка); B-02 → «вывод из кода» | F-12/F-13/§5 |

**Повторю условие пересмотра вердикта:** если после этой волны квитанции не будут перезаписаны на финальном дереве (F-01), required_evidence маршрута остаётся `stale` — тогда вердикт обязан быть **fail**, независимо от качества текстов.

---

## Инкорпорирование (писал владелец прогона, 07:3x UTC; verdict ревьюера выше не изменён)

| Находка | Статус | Чем закрыто |
| --- | --- | --- |
| F-01 ревью-receipt'ы привязаны к старому HEAD | закрывается этим прогоном | порядок: финальный коммит → `grok_verify` → `code_review`/`test_review` на одном fingerprint; отдельного детектора свежести ревью-квитанций в гейте нет — стековый дефект, заведён как #169 |
| F-02 §10 ссылается на удалённые `/tmp`-логи | закрыто | `evidence/factory-postgres-exit-20260920.log`; статистика flaky честно помечена «артефакта нет» |
| F-03 четыре числа не порождены ни одним инструментом | закрыто | `evidence/v3_measurements.py` (rc=0 ⇔ отчёт совпадает; 11 контролей переворачиваются) + `evidence/harness/` со `REAL MAPS ok=125/125` |
| F-04 `linux-static-audit.md` не воспроизводится побайтно | закрыто | генератор нормализует EOF; повторный запуск даёт идентичный файл, `git diff --check` чист |
| F-05 ни один гейт не исполняет измерители, `ruff` исключает `engineering` | не закрыто в этом маршруте | слой стека: #169 (PASS по пустому скоупу, 0 % покрытия продукта); конфиг гейта в review-only маршруте не правится |
| F-06 NFR «checks must be rerunnable» | закрыто вместе с F-04 |
| F-07 §9.4 требует кейс «падение на 16 px», которого в данных нет | закрыто | v3 P1-2 (59 подъём / 29 падение / 37 без изменения); хендаут и probe используют эти числа |
| F-08 «контроль 0/370» | закрыто | v3 §4: 3/370 (+20/370 при переборе систем координат) |
| F-09 «тип 15 = конец стадии» без тега про смещение | закрыто | v3 §3 сохраняет уточнение dx=+2 |
| F-10 критерии жили только в markdown | закрыто | `change-spec.yaml` типизирует AC-009…AC-029 + карта id в `requirements.md`; паттерн стека `AC-\d{3}` не принимает `AC-F-*` (проверено падением рекордера) |
| F-11 коммиты аудита вне merge-полномочий | закрывается PR этого прогона | ветка `codex/factory-updated-20260920` → PR, первой строкой ссылка на фабрику |
| F-12 отозванные числа мутационного контроля без эратума | закрыто | датированный текст не переписывается; противоречие снято в v3 §4 и в `decisions.md` |
| F-13 §1.6/§6 устарели | закрыто | v3 §7 пересчитан; untracked-записи сокращены `.gitignore` |
| F-14 `state.json` утверждал PASS раньше артефактов | закрыто | журнал дополнен реальными метками и оговоркой о неподтверждённых 01:46-записях |
| F-15 границы маршрута соблюдены | подтверждено заново | `git diff 403eb13..HEAD -- Exolon Exolon.xcodeproj README.md` пуст |
