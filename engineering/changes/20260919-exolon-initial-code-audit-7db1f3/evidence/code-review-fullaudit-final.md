# Финальное независимое ревью доставленного пакета «Полный аудит Exolon» (`code_reviewer`)

**Маршрут:** `7db1f3f0b126` (intent=`review`, write_agent=null, quality=`base`)
**Ревьюируемое дерево:** HEAD `52795d1`, base `403eb13`, дельта = `b8aee42` (factory) + `bcacf30` (аудит) + `52795d1` (whitespace)
**Объект:** документальный пакет (`engineering/reports/exolon-full-audit-20260920.md`, баннеры в двух v1-отчётах, `evidence/fullaudit_measurements.py`, `evidence/fullaudit-b01-spawn.json`), НЕ продуктовый код.
**Режим:** read-only. Единственный записанный файл — этот отчёт. Все измерения ниже выполнены заново в этом дереве; скрипт замеров прогонялся в изолированном зеркале (`/tmp/probe` + symlink на `Exolon/`), чтобы не трогать репозиторий.

## Вердикт: pass

Вердикт: pass — с двумя обязательными правками до мержа (F-1, F-2; чисто документальные, продуктовых выводов они не меняют)

Центральное утверждение пакета — **опровержение единственного P0 (C1)** — **верно и воспроизводимо**: я независимо пересчитал геометрию, сетку collision и мутационный контроль, и всё сошлось до пикселя. Все 17 spot-check'ов нагрузочных чисел (§1/§3/§4/§5/§6) воспроизвелись; расхождения найдены только в **распространении коррекции** (`decisions.md`), в **актуальности §6 относительно коммита, который сам его и опровергает**, в **отсутствии PR на доставляемую ветку** и в ряде неточных цитат/знаменателей. Ни одного места, где Linux-статика выдаётся за сборку/геймплей, не найдено; ни одна цифра не оказалась вымышленной.

---

## 1. Ключевой вопрос: оправдано ли опровержение C1? — ДА

Проверка независимая и многослойная: код → геометрия → сырая сетка → прогон артефакта → сравнение реплики с оригиналом; всё сходится.

| Шаг | Что сделано | Результат |
| --- | --- | --- |
| Формула | `sed -n '295,306p' Exolon/GameCore/Levels/TMXLevelRuntime.swift` | `x=minX-16`, `y=max(0,minY-16)`, `w=max(96,w+64)`, `h=height+32` ✓ как в отчёте |
| Геометрия | python-разбор `Exolon/Resources/L01S10.tmx`: `capsule x=368 y=176 w=32 h=80`, props `coordinateMode=tiledRect`; `pixelHeight=384`; `TMXMapLoader.swift:73-80` | trigger `(368,128,32,80)` → **y=128..208**; exclusion **(352,112,96,112)** → **y=112..224** — ровно как §1/§3 |
| Сетка | послойный дамп collision-слоя (35×24) по строкам, csv-сплит `[,\s]+` | будочные клетки **gid=1, колонки 23–27, строки 11–15 (y=128..208) — солидны**; строки 16–17 (y=96..128) — **пусты**; пол — **строки 18–19 = y=64..80 и y=80..96, солидны на всю ширину x=272..560** ⇒ «две строки y=64..96, верх 96», запас до границы 112 = **16 px** ✓ |
| Токенизатор-эррата | `tokens by [,\s]+ = 840`, `tokens by \n only = 840`, `W*H = 840` | уникальность L01S10 (без завершающих запятых) подтверждена, корректный сплит даёт полные 840 ✓ §2 |
| Мутационный контроль | прогон `fullaudit_measurements.py` + собственный свип литерала `16` (16/17/31/32/33/48/49) | shipped `−16` → пол цел (1 rect); порог **95 px** (=смещение на 33) → пол **прорезан** (0 rect'ов); **`−32` (низ=96) пол НЕ режет** (впритык) ⇒ контроль **не слепой**, формулировка §2 «смещение порога до ≤95 px прорезает пол» ✓ точна |
| Воспроизводимость артефакта | `python3 fullaudit_measurements.py` (в зеркале) → `rc=0`, `ALL_MEASUREMENTS_MATCH_REPORT`; `md5sum` перегенерированного `fullaudit-b01-spawn.json` против закоммиченного | `28e1b784eb5c6738e42e2372939081d0` = **идентично** ✓; cabin `5/1/0/1/0`, beams `10/10`, b01 `35 exact / 89 void / 1 16_above`, total 125 ✓ |
| Реализация python-реплики vs Swift | сверка `solid_rects`/`subtract` с `TMXTileMapRenderer.buildCollisionRects` (:115-146) и `TMXLevelRuntime.subtract` (:447-479) | декомпозиции различаются формой кусков, но **множество «rect \\ overlap» идентично**; для rect'ов, целиком внутри/снаружи полосы исключения, результаты совпадают ⇒ реплика faithful |

**Вывод:** первичный P0 был ложноположительным (стены «будки» приняты за пол); понижение до P3 с остатком «4 кабины с запасом 0 px» — корректно. Независимый расчёт маржи по всем 125 картам (`capsule` + `source_marker/changing_room`): `capsule` → **16.0 px**, `L02S10`/`L03S11`/`L04S16`/`L05S10` → **0 px**, ровно 4 штуки ✓ (зоны 034/060/090/109 по формуле `(level-1)*25+(stage-1)` ✓). Дамп в `analysis-architect-fullaudit.md:117-121` совпадает с моим замером.

---

## 2. Находки (по убыванию серьёзости)

### F-1 · Medium · `decisions.md:9` — коррекция не дошла в shared memory, там живёт опровергнутая цифра

Текст: *«…with a mutation control (**shifting the exclusion 32/48 px does cut floor cells**; the shipped formula does not)»*. Это ровно те числа, которые отчёт в §2 (`exolon-full-audit-20260920.md:32`) сам же и снял эрратой («неверные числа „мутационного контроля“ у архитектора (−32→1, −48→2)»).
Мой замер (свип литерала `16` в таблице §1): при `minY-32` нижняя грань = 96 = верх пола, пересечения нет → **пол цел**; режет только с `minY-33` (низ 95) и глубже. То есть «32 px … does cut» — **ложь**, «48 px» — правда.
`decisions.md` — файл общей памяти, обязательный по `AGENTS.md:32`, и он изменён тем же коммитом `bcacf30` (+4 строки): следующий агент получит опровергнутую «доказательную» цифру как факт.
**Command:** `sed -n '5,10p' decisions.md`; прогон реплик `solid_rects`/`subtract` из `evidence/fullaudit_measurements.py` со смещением нижней границы исключения 16/17/31/32/33/48 px — считать нужно было на копии дерева (сам скрипт пишет `fullaudit-b01-spawn.json` рядом с собой, а `import` оставил бы `__pycache__` в `evidence/`).
**Правка:** заменить скобку на «смещение нижней границы до ≤95 px (−33) прорезает пол; shipped −16 — нет» и, по `AGENTS.md:7`, завести `mistakes.md` (этого файла в дереве нет — см. §3).

### F-2 · Medium · §6 «Гигиена и git-состояние» опровергнута коммитом, который её же доставляет; у пакета нет PR

Строка `:75` («**доказательства нигде, кроме рабочего дерева**»; «8 untracked-записей … оба отчёта `engineering/reports/`, пакет аудита 7db1f3») и строка `:78` («текущая локальная ветка `codex/factory-updated-20260920` — её **неотправленный** однофамилец; upstream-трекинга нет ни у одной локальной ветки») были верны на `b8aee42`, но **неверны на HEAD доставленного пакета**:
- `git ls-files engineering/reports` → 3 файла (все три отчёта закоммичены в `bcacf30`; `git show --stat bcacf30` = 31 файл, +10720) — т.е. тезис «доказательств нет в git» опровергается коммитом, в котором живёт сам отчёт;
- `git branch -vv` → `* codex/factory-updated-20260920 52795d1 [origin/codex/factory-updated-20260920]`; `git rev-parse HEAD origin/codex/factory-updated-20260920` → один и тот же SHA — ветка **отправлена**, трекинг **есть**;
- сегодняшние 8 untracked-записей — это `.grok-stack/runtime/*`, два `__pycache__`, `.qwen/`, и к отчётам/пакету отношения не имеют.
Практическое следствие: `gh pr list -R Dimkox/exolon --state all --json number,state,isDraft,headRefName` → **только** PR #1 (OPEN, DRAFT, head `codex/update-factory-20260920` = `b8aee42`). Коммиты аудита (`bcacf30`, `52795d1`) не покрыты ни одним pull request'ом, хотя `AGENTS.md` («PR-only delivery») и `route.delivery_expected=true` этого требуют. При этом §7 озаглавлен «v2 — **единственный актуальный**», а §7 P3 перечисляет «hygiene-таблица §6» как живой бэклог — то есть в circulation остаётся таблица, уже опровергнутая собственным деревом.
**Command:** `git ls-files engineering/reports && git show --stat bcacf30 | head -3 && git branch -vv && gh pr list -R Dimkox/exolon --state all`
**Правка:** добавить в §6 строку-дельту («изменено коммитами bcacf30/52795d1: отчёты и пакет теперь в git; ветка отправлена; PR на ветку аудита не открыт») и открыть PR на `codex/factory-updated-20260920`.

### F-3 · Low-Medium · §6:76 — мандат «AGENTS.md ссылается на…» приписывает манифесту два пункта, которых в нём нет

Отчёт относит к `AGENTS.md` семь отсутствующих целей. Проверено: `START_HERE.md`, `PROJECT_STATE.json`, `mistakes.md` (`AGENTS.md:7,11,32,55`), `architecture/*` (`:27`), `trust-ci/` (`:20`) — **да**, перечислены и отсутствуют. Но:
- `VERSION` в `AGENTS.md` встречается только как проза «current VERSION» (`:26`), не как путь; как control-plane путь `VERSION` значится в `.grok-stack/config/policy.json` (и в `analysis-repo_explorer-fullaudit.md:279` это описано корректно);
- `scripts/install_into.py` в `AGENTS.md` **не упоминается** вообще: `grep -rn "install_into" .` → только `.grok-stack/config/policy.json:21,64` и `.grok-stack/adaptive_grok/_policy_legacy.py:22`.
Существо находки (обязательный entrypoint неисполним; декларированные control-plane файлы отсутствуют) — верно и подтверждено `ls`; страдать будет только перекрёстная ссылка: читатель, который пойдёт править `AGENTS.md` по этому пункту, не найдёт в него чего править.
**Command:** `grep -n "install_into\|VERSION" AGENTS.md` / `grep -rn "install_into" --include=* . | grep -v '^./.git/'`

### F-4 · Low-Medium · §1.5:16 — «контроль 0/370» сильнее, чем данные: там 3 исключения, и они описаны в том же evidence

`970`, `125`, `145`, `72`, `73`, `370`, `solid= 0/125` — все воспроизвелись моей реализацией джойна (regular-разбор `LEVEL_COMPILER_AUDIT.md` + `sourceX/sourceY` из 125 TMX). А вот «контроль 0/370» — нет: из 370 объектов **без** `sourceX/Y` случайно матчатся **3** (`L01S10/capsule`→type 12, два `L01S10/piston`→type 10). Источник `analysis-docs_researcher-fullaudit.md:159` честно пишет «370 из 370 (**кроме 3**)» и «контроль сработал в обе стороны» — сводный отчёт, объявленный «единственным авторитетом», это «кроме 3» потерял. На вывод D-01 не влияет (370/3 ≈ нетривиальное расхождение), но завышает чистоту фальсифицирующего теста.
**Command:** python-свип: `(int(x)//16, int(y)//16) ∈ tuples[zone]` для объектов без `sourceX/Y` → `accidental matches: 3`.

### F-5 · Low · §4:49 и §9:98 — «90 без локального пола» против собственного JSON: 90 = «не точно», без пола — 89

Закоммиченный `evidence/fullaudit-b01-spawn.json`: `exact 35`, `void 89`, `16_above 1` (это `L03S11.tmx` — пол есть, он на 16 px **ниже** ног). Значит «90/125 не согласованы» ✓, но «90 без локального пола» ✗ (без пола — 89). Смежный дефект формулировки: §10:110 утверждает «**rc=0 ⇔ все числа совпадают с отчётом**», тогда как гейты скрипта проверяют только `booth=5/floor=1/after=0/1/control=0`, `beams 10/10` и `total=125 & exact=35` — разбивка 89/1, «27 имён», числа §5/§6 и джойн §1.5 под `ok` не лежат. Бывшее ревью (`code-review-fullaudit.md`, строка 6) эту деталь увидело, но формулировку отчёта не поправило.
**Command:** `python3 -c "import json,collections;d=json.load(open('.../fullaudit-b01-spawn.json'));print(collections.Counter(d.values()))"` → `void 89, exact 35, 16_above 1`; и чтение `measure_b01()` (последние 3 строки функции).

### F-6 · Low · §2:32 и §3:38 ссылаются на «полный ASCII-дамп collision-сетки», которого нет в дереве

Оба места называют разрешающим доказательством «полный ASCII-дамп сетки collision (строки 10–21)». В дереве такого артефакта нет: `grep -rlnE "[.#]{12}" engineering/changes/.../evidence/` → пусто; `fullaudit_measurements.py` дамп сетки не печатает (только счётчики rect'ов). Коммитированный эквивалент — rect-уровневый разбор `analysis-architect-fullaudit.md:90-110` («removes rect x=368 y=192..208 w=80 → remains 0» ×5). Факт при этом проверяем за одну команду (я его восстановил), так что это дефект ссылки, а не достоверности.
**Command:** `grep -rn "ASCII" engineering/reports/exolon-full-audit-20260920.md` + `grep -rlnE "[.#]{12}" engineering/changes/20260919-exolon-initial-code-audit-7db1f3/evidence/`

### F-7 · Low · §10:111 — доказательная база самого тяжёлого вывода (201/201, flaky 1/3, PG 17.11) лежит в `/tmp`, и её уже нет

`ls /tmp/exolon-factory-exit*.log` → **файлов нет** (даже на этом хосте). Следом непроверяемы: «201/201 без скипов в чистом контейнере», тайминги 251/173/163 s, «1 ERROR под загрузкой (1/3)». Что воспроизводится из дерева: `python3 -m unittest discover -s factory/tests -t .` → `Ran 201 tests … OK (skipped=71)` ✓ (т.е. сам счётчик 201 и исчезновение `ModuleNotFoundError` из v1-строки `exolon-initial-audit.md:48` — подтверждены); существо flaky подтверждено внешним носителем — `gh issue view -R Dimkox/adaptive-grok-build-pro 155` → OPEN, заголовок про `bind_repair_child` + «load-flaky (1/3)» ✓, `#157` → OPEN, про отсутствие Swift-сигнала в классификаторе ✓. Второй класс того же: §1.5/§3:40 («27 имён `SKTexture(imageNamed:)`», «970 кортежей») порождены heredoc'ами из `/tmp/docsaudit/*`, которые не закоммичены как скрипты (текст команд есть inline в evidence). Я пересобрал оба измерения: 27 имён, **все резолвятся на диске** ✓; 970/72/370/0-125 ✓.
**Command:** `ls /tmp/exolon-factory-exit*.log`; `python3 -m unittest discover -s factory/tests -t . | tail -3`; `gh issue view -R Dimkox/adaptive-grok-build-pro 155 --json state,title`
**Правка:** либо положить логи/их извлечение в `evidence/`, либо понизить формулировку §1.4 до «прогон 2026-09-20 на хосте X; воспроизводится командой Y; носитель — upstream #155».

### F-8 · Low · §5:65 и §5:68 — два знаменателя не сходятся

- «`factory/contracts` заполнен (**28 jsonschema** + 7 openapi)»: `find factory/contracts/jsonschema -type f | wc -l` → **27** (+7 в `openapi/`, +4 в отдельном `contracts/schemas/`; всего 38). Метод подсчёта не назван → число невоспроизводимо ни одной командой.
- «factory/README.md: **7 из 8** относительных ссылок битые»: python-разрыв markdown-ссылок → 11 относительных вхождений, 8 битых вхождений, **7 битых уникальных** из **10 уникальных**. Числитель верен, знаменатель — нет (это же отметили прошлое ревью, строка 18, как «minor»).
Остальное в §5 сходится точно: 25 413 / 21 071 / 21 105 / 13 368 LOC (`find … ! -path '*__pycache__*' | xargs wc -l`) ✓; отношение к продукту `59 852/4 227 = 14.16×` ✓; `sha256sum factory/src/adaptive_factory/landing_http.py` = `21c03d67…ca01ddc` = манифестному `changed_managed_files` ✓; `git diff --shortstat 403eb13 b8aee42` → **24 files, +1477/−4** ✓; `git diff --stat 403eb13 b8aee42 -- Exolon Exolon.xcodeproj` → **пусто** ✓; версии v2.0.13/v2.0.14 (`factory/README.md:14,16`), `pyproject version = "0.1.0"` (`factory/pyproject.toml:7`), `product_version: "2.0.18"` (`engineering/runbooks/factory-source.json`) ✓.

### F-9 · Low · Баннеры корректны, но перечисляют не всё опровергнутое (ответ на вопрос №3 брифа)

`exolon-initial-audit.md:3-14` и `exolon-initial-audit-backlog.md:3-8` делают ровно то, что от них требуется: снимают **C1/C1b** с той же арифметикой, что и полный аудит (будка `y=128..208` солидна и вырезается по замыслу; пол `y=64..96`, верх 96; граница 112; запас 16 px; 4 кабины с 0 px), и **не** переписывают датированное свидетельство: тело v1 осталось дословным (`exolon-initial-audit.md:335` по-прежнему содержит строку `| C1 | P0 | Подтв. код / гипотеза play |`, `:433` — прежнюю рекомендацию правки) ✓; в бэклоке прежние P0-строки живы на `:19-20` (`| P0 | C1 |`, `| P0 | C1b |`) ✓. Полного аудита в них не превращают, ссылку на авторитет дают ✓.
Претензия одна: баннер перечисляет «здесь опровергнуто: C1» + «неверна интерпретация §9.1», но **не** упоминает снятие **V3** («5 потерянных ресурсов вне pbx» → «не дефект»), которое живёт в той же v1-таблице (`exolon-initial-audit.md:349`) и в том же бэклоге (`backlog.md:37`, `| P3 | V3 | packaging | 5 ресурсов вне pbx Resources |`). Формальная страховка есть («Остаток документа — исторический снимок 19.09 без гарантий актуальности»), так что это низкая северити; добавить одной строкой.
**Command:** `grep -n "C1\|V3" engineering/reports/exolon-initial-audit.md | head`; сверка тел: `diff <(git show bcacf30:engineering/reports/exolon-initial-audit.md) <(git show 52795d1:engineering/reports/exolon-initial-audit.md)` → расходятся только hard-break маркеры (F-10).

### F-10 · Info · `52795d1` и ещё два мелких расхождения описания

- Месседж `52795d1` («normalize trailing whitespace») занижает механизм: в 7 файлах (52 строки) markdown hard break «два пробела» заменён на **backslash** (`'**Дата:** 2026-09-19  '` → `'**Дата:** 2026-09-19\\'`), плюс удалён один хвостовой пустой ход в `linux-static-audit.md`. Рендер сохранён, содержательных правок нет, но `git diff -w bcacf30 52795d1` **не** пуст — потому «whitespace-only» теперь не доказывается привычным флагом. Сама цель коммита достигнута: `git diff --check 403eb13 HEAD` → **чисто** ✓.
- §6:78 «два внешних worktree в `~/.exolon-factory-update-20260920`» → `git worktree list` даёт `<worktree>/{exolon,verified-exolon}` (количество верно, путь нет).
- §1.2/§2 «Swift 6.4.0» против фактического `swiftc --version` → `Swift version 6.4 (swift-6.4-RELEASE)` (тривиально, прошлое ревью отмечало).

---

## 3. Ответ на вопрос №4: манифестная честность

Отсутствуют на HEAD (проверено `for p in …; do [ -e $p ]` и `git ls-files | grep`): `START_HERE.md`, `PROJECT_STATE.json`, `mistakes.md`, `VERSION`, `architecture/` (`system.yaml`, `rules.yaml`, `generated/`), `trust-ci/`, `scripts/install_into.py`, корневой `.gitignore` — все восемь ✓. Пустые каркасы: `engineering/adr`, `engineering/reviews`, `engineering/contracts/{openapi,asyncapi,schemas}` ✓ (`find engineering -type d -empty`).
Что §6 передаёт **точно**: отсутствие `.gitignore` ✓, отсутствие shared `.xcscheme` ✓ (`find . -name '*.xcscheme'` → пусто; в `Exolon.xcodeproj` ровно `project.pbxproj`), «XCTest у игры: 0 файлов» ✓ (`grep -rl XCTest --include=*.swift .` → пусто), пустые скелеты ✓, «PR #1 (DRAFT)» ✓ (жив, OPEN/draft на `b8aee42`).
Что §6 передаёт **неточно**: состав untracked и статус отправки ветки (F-2), атрибуция `VERSION`/`install_into.py` `AGENTS.md` (F-3).
Отдельно: `README.md` не содержит architecture-ссылок, обязательных по `AGENTS.md:27`, — факт есть в `analysis-repo_explorer-fullaudit.md:279`, но в сводный §6/§7 он не попал.

## 4. Что воспроизведено успешно (spot-check'и, ≥6 обязательных)

| # | Утверждение (где) | Команда | Итог |
| --- | --- | --- | --- |
| 1 | 18/18 Swift через `swiftc -frontend -parse` (§1.2) | цикл по `find Exolon -name '*.swift'` + контроль-битый-файл | **18 файлов, 0 fail**; контроль rc=1 ✓ |
| 2 | 125/125 TMX well-formed (§1.3) | `for f in Exolon/Resources/*.tmx; do xmllint --noout $f` | **ok=125 bad=0**; контроль rc=1 ✓ |
| 3 | 153 PNG целостны (§2) | цикл `pngcheck -q Exolon/Resources/*.png` | **153, rc=0**; контроль rc=2 ✓ |
| 4 | 125 зон / 18 Swift-файлов / 153 PNG (§1.3, §6) | `find … \| wc -l` | 125 / 18 / 153 ✓ |
| 5 | B-01: 35 exact + 90 (§4:49) | прогон скрипта + `md5sum` JSON | 35 exact ✓, 90 не-exact ✓, но «без пола» = 89 (F-5) |
| 6 | O7: 10/10 карт up+down, «50 вместо 25» (§3:41, §4:56) | перебор `sourceBlock=blk_beam_*` + геометрия box'ов + `LevelObstacles.swift:766 hitPoints = 25` | 10/10 ✓; в каждой из 10 карт поля up/down **перекрываются на 32×160 px** ⇒ 2×25 ✓ |
| 7 | O8: «19 объектов на 18 картах» (§4:55) | python-подсчёт `name="double_launcher"` | **19 / 18** ✓ (формулировка родителя «18 на 19» перевёрнута; отчёт прав) |
| 8 | D-02: 5 первых зон без `imagelayer` (§4:58) | `'\<imagelayer' not in text` | `L01S01..04, L01S07` = зоны 000–003, 006 ✓ |
| 9 | D-03: tiledRect 1/125; `max(96,w+64)` :304 vs `w+32` :377 (§4:60) | grep по TMX + `sed -n '300,306p'/'372,380p'` | 1 файл (L01S10) ✓; обе строки на своих местах ✓ |
| 10 | B-02: `L05S16` — «вся коллизия один rect x=0..368» (§4:50) | дамп collision | ровно одна строка solid, x=0..368 (23 клетки), колонки 23–34 пусты ✓ |
| 11 | B-05: `loadCheckpoint()` не вызывается; 7 ключей `Exolon.Step10.*` :25-31 | `grep -rn loadCheckpoint Exolon` | 1 совпадение = определение ✓; 7 ключей ✓ |
| 12 | V1/V2: 0.5 vs 0.3 при `GENERATE_INFOPLIST_FILE=NO` | grep pbxproj + `Exolon/Resources/Info.plist:18` | ✓ (`Info.plist` лежит в `Resources/`, не в корне) |
| 13 | §1.5: 970 кортежей / 72 совпадения / solid 0/125 | собственная реализация джойна | 970 ✓, 125 строк ✓, 145 объектов с `sourceX/Y` (72+73) ✓, 370 в контроле ✓ (3 исключения — F-4), solid 0 из 125 ✓ |
| 14 | §5: LOC-числа, дельта factory, sha манифеста | `wc -l`, `git diff --shortstat`, `sha256sum` | ✓ (кроме «28 jsonschema», F-8) |
| 15 | §1.4 «100 тестов / 3 ERROR» закрыт | `python3 -m unittest discover -s factory/tests -t .` | `Ran 201 … OK (skipped=71)`, ошибок импорта нет ✓ |
| 16 | Внешние issues #155/#157 | `gh issue view` | OPEN, заголовки соответствуют тезисам ✓ |
| 17 | «try!/as! — во всём дереве ноль», gif-декодера нет | `grep -rn "try!\|as!" Exolon \| wc -l`; `grep -rn "\.gif" Exolon` | 0 ✓; `.gif` только в комментариях кода (3 файла в Resources) ✓ |

## 5. Что осталось за пределами проверяемости (не претензия к отчёту)

Тип-чек и сборка Swift, фактическое поведение кабины/полов/бонусов, «ферма» B-03, залипание геймпада B-06, версия образа `postgres:17-alpine` (17.11), PGP-статус ключа подписи Swift (§8) — всё это либо вынесено в §9 macOS-чеклист, либо относится к провижинингу хоста. Отчёт ни в одном месте не выдаёт это за подтверждённое; колонка «Статус» (§4) различает «код+данные» и «play-гипотеза», а B-04 прямо помечен «закрыто статикой, отдельного play-пункта нет намеренно» ✓.

## 6. Итог для родителя

- Ключевая ставка пакета — **опровержение C1 — верна**, доказана воспроизводимым артефактом с настоящим (не слепым) мутационным контролем, и мой независимый пересчёт геометрии/сетки/маржи совпал с отчётом полностью.
- Продуктовых выводов найденные дефекты не меняют; ни одного «выдуманного» числа или висячей file:line-ссылки нет (в отличие от прошлой волны — теперь все 12 адресов бьются, см. §6 test-review).
- Обязательные до-мерж правки (только документальные): **F-1** (`decisions.md`), **F-2** (строки §6 + PR на ветку аудита). Остальное — точность формулировок/знаменателей (F-3…F-10), пригодное для пакетной чистки одним коммитом.
- **Вердикт: pass** при условии, что F-1 и F-2 устранены до записи финальных квитанций; без них следующий агент получает опровергнутую цифру как доказательство и таблицу, которую опровергает само дерево.

---

## Инкорпорирование (писал владелец прогона, 07:3x UTC; verdict ревьюера выше не изменён)

| Находка | Статус | Чем закрыто |
| --- | --- | --- |
| F-1 `decisions.md` про контроль 32/48 px | закрыто | формулировка исправлена на «33 или 48 px прорезает; 32 px ставит границу ровно на верхний край пола и не прорезает» |
| F-2 §6 «отчёты не трекаются / ветка не отправлена» | закрыто | v3 §7 пересчитан на финальном дереве; в v2 добавлен баннер о устаревшем разделе |
| F-3 атрибуция `install_into.py` файлу `AGENTS.md` | закрыто дважды | в v2 баннер дополнен позицией про `:87` (тело датированного раздела не переписывается); носитель — `policy.json:21,64` и `_policy_legacy.py:22`; в опубликованном issue фабрики #161 вынесен эратум, файл доказательств перегенерирован с колонками «названо в AGENTS.md / названо в policy.json / существует на HEAD» |
| F-4 «контроль 0/370» | закрыто | v3 §4: 3/370, плюс перебор 8 систем координат (максимум 20/370) |
| F-5 «90 без локального пола» как единственная мера | закрыто | v3 P1-2 и `evidence/v3_measurements.py`: обе меры в одном артефакте (35 legacy / 37+59+29 по правилу кода) |
| F-6 ссылка на несуществующий ASCII-дамп | закрыто | v3 §4 помечает отсутствующий артефакт и указывает заменитель |
| F-7 `/tmp`-логи factory-контура | закрыто частично | детерминированная часть закоммичена (`evidence/factory-postgres-exit-20260920.log`); статистика flaky 1/3 помечена «артефакта нет» |
| F-8 28 jsonschema / 7 из 8 ссылок | закрыто | v3 §4: 27 и «7 битых из 10 уникальных (11 вхождений)» |
| F-9 баннеры не перечисляют V3/O7/O8/V2 | закрыто | в v1-отчёт и бэклог добавлен блок «v3-уточнение» |
| F-10 характер коммита `52795d1` | принято к сведению | реальный эффект (срезанный EOF порождённого файла, ломающий воспроизводимость и `git-diff-check`) описан в v3 §8 и исправлен в генераторе |
