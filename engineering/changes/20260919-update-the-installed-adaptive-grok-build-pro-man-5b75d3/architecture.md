# Bounded vendor update

No architecture, schema, dependency or runtime-service changes. Rejected provider drafts retain their validated response digest and expose allowlisted draft failure codes while preserving needs_human state and existing fallback eligibility.

The generic installer profile owns 346 paths. Upstream handoff documents and live-service operational records are not consumer payload. Leave AGENTS and all other managed files unchanged. The route frontend label adds no UI work: this is a Python vendor-module update.

The full verifier exposed a preexisting consumer packaging defect: installed API/PostgreSQL tests import test_execution_contracts and test_execution_service, which the installer omits. Restore those two unchanged test modules from the same pinned upstream source as supplemental verification support. This is not a change to runtime behavior or the 346 managed payload entries. No check is disabled.
