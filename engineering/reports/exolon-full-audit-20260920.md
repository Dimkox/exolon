# Полный аудит Exolon — 2026-09-20

**HEAD:** `b8aee42` (ветка `codex/factory-updated-20260920`; продукт идентичен `403eb13`)
**Маршрут:** `7db1f3f0b126` (intent=`review`, write_agent=null) · **Пакет:** `engineering/changes/20260919-exolon-initial-code-audit-7db1f3`
**Хост:** Ubuntu 24.04 x86_64. Xcode/macOS по-прежнему нет; в этот раз добавлены Swift 6.4 (Linux), xmllint, pngcheck, живой PostgreSQL-контур factory.
**Документ заменяет по авторитетности** `exolon-initial-audit.md` от 19.09 (в него и в бэклог добавлены корректирующие шапки).

---

## 1. Итог

1. **Единственный P0 первичного аудита (C1 «exclusion кабины режет пол») ОПРОВЕРГНУТ.** Постройтовый замер `L01S10.tmx` корректным токенизатором: клетки-«будка» (gid=1, колонки 23–27, строки 11–15, `y=128..208`) в Collision-слое **солидны**, и exclusion `(352,112,96×112)` вырезает именно их — это замысел (будка проходима, README). Пол под будкой — две строки `y=64..96` (верх 96); нижняя граница исключения (112) до него не дотягивает, **запас ровно 16 px**. Play-гипотеза «провал сквозь кабину» не имеет механизма. C1 → P3 (robustness: у 4 маркерных кабин геометрия другой ветки даёт запас 0 px; D-03).
2. **Продукт синтаксически здоров:** впервые все **18/18 Swift-файлов** прогнаны через `swiftc -frontend -parse` (Swift 6.4.0) — PASS. Тип-чек и сборка невозможны без Apple SDK — это потолок Linux-валидации.
3. **Данные целы:** **125/125 TMX** well-formed (xmllint — независимый парсер от python-разбора первичного аудита); все PNG целостны (pngcheck); контроль «битый вход обязан падать» прошёл для обоих инструментов.
4. **Factory-оверлей (никто его ранее не аудитовал) — 25 413 LOC Python в `factory/src`, 21 071 LOC policy-кода в `.grok-stack/adaptive_grok` (21 105 на весь `.grok-stack`), 13 368 LOC тестов**, т.е. в ~14 раз больше продукта. Полноценный прогон обязательных тестов впервые: **201/201 OK в чистом disposable-контуре** (PostgreSQL 17.11 через docker). Один невоспроизводимый ERROR под загрузкой хоста (1/3 прогонов) → заведён upstream **issue #155**. Прошлый «100 тестов / 3 ERROR» первичного аудита закрыт обновлением factory (ModuleNotFoundError исчез вместе с b8aee42).
5. **Интерпретация §9 первичного аудита («1:1 по типам компилятора») неверна системно:** координатный джойн 970 кортежей `LEVEL_COMPILER_AUDIT.md` с объектами 125 TMX (72 точных совпадения по `sourceX/sourceY`, контроль 0/370 на нерелевантном индексе) доказывает: документ описывает **вход ASM-компилятора**, а не привезённые уровни. Тип 11 = нижние пушки (`gunMachine_BOTTOM` 18/18), не «56 rocket launchers»; тип 15 = конец стадии, не HV; `solid=` не воспроизводится ни при одной дефиниции (0/125). Числовые совпадения имён (`turret=37` и т.п.) остаются валидными как «имя↔данные», но не как «тип↔документ».
6. **Гигиена репозитория — дыра:** корневого `.gitignore` нет вовсе; `AGENTS.md` обязывает читать несуществующие `START_HERE.md`, `PROJECT_STATE.json`, `mistakes.md`, `architecture/`, `trust-ci/`; `engineering/{adr,reviews,contracts/*}` — пустые каркасы; доказательства первичного аудита живут только в рабочем дереве (untracked).
7. **Вердикт по продукту не изменился по существу:** это честный Step 9-чекпоинт-импорт, а не порт оригинала; все «недоделки оригинала» (нижние пушки, ракетные башни, таблицы врагов, преследователь, полный бонус) остаются недоделками. Добавились новые реальные дефекты ремейка (см. §4).

---

## 2. Метод и инструментарий (что установлено в этом прогоне)

| Инструмент | Установка | Что дал |
| --- | --- | --- |
| `libxml2-utils` (xmllint) | apt | well-formedness 125/125 TMX, независимо от python-парсера |
| `pngcheck` | apt | целостность 153 PNG в Resources |
| Python: `fastapi 0.128.2`, `httpx`, `uvicorn`, `psycopg[binary]`, `pypdf`, pytest-стек из `.grok-stack/config/python-test-requirements.txt`, editable `adaptive-factory` | pip --user | снятие 9 ModuleNotFoundError-ошибок прогона 19.09 |
| Docker + `postgres:17-alpine` (17.11) — уже были | — | первые живые 71 postgres-интеграционных теста factory |
| **Swift 6.4.0** (tarball swift.org, PGP **Good**, ключ подписи истёк 16.09.2026 — см. §8) | распакован в `/opt/swift`, profile.d | `swiftc -frontend -parse`: 18/18 PASS |

Правило контроля: каждый валидатор имел заведомо падающий кейс (битый XML, побитый PNG, сломанный Swift — все корректно упали). Ложная тревога прогона тоже задокументирована: первичная «обрезанная» метрика 817/840 для L01S10 была дефектом **моего** токенизатора (строки CSV L01S10 не имеют завершающих запятых — уникальный файл; корректный сплит даёт ровно 840). **Эррата:** тот же баг исказил и мои промежуточные постройтовые дампы колонка-за-колонкой (off-by-one от склейки строк), из-за чего первые версии §1/§3 содержали неверные «64..80 / не солидны»; финальный разрешающий замер — полный ASCII-дамп сетки collision (строки 10–21), его цифры и стоят в отчёте. Тот же дефект породил неверные числа «мутационного контроля» у архитектора (−32→1, −48→2); faithful-мутация независимой проверки: смещение порога до ≤95 px прорезает пол — неверность оригинального C1 это не меняет. Swift-загрузчик режет и по `\n`, и по `,` — устойчив.

## 3. Пересмотр выводов первичного аудита

| Первичный вывод | Новая оценка | Доказательство |
| --- | --- | --- |
| **C1 (P0)** exclusion режет пол | **Опровергнут → P3** | финальный ASCII-дамп collision-сетки `L01S10`: будочные клетки (колонки 23–27, `y=128..208`) солидны и вырезаются исключением — по замыслу; пол `y=64..96` (верх 96) остаётся нетронутым с запасом 16 px. Первичный аудит принял «стены будки» за «пол» |
| §9.1 «1:1 тип↔маркер» | **Ошибка интерпретации** | координатный джойн doc↔TMX: 5 семейств раскладываются 100% только при переприсваивании типов (11=BOTTOM, 15=stage_end); `solid=` 0/125 |
| **V3** 5 «потерянных» ресурсов вне pbx | **Не дефект** | все 27 имён `SKTexture(imageNamed:)` резолвятся на диске; настоящая дыра — GIF-анимация: декодера нет, gif-имена только в комментариях кода |
| **O7** двойное поле (1 карта) | **Уточнён: 10/10 карт** с beam-маркерами имеют обе ветки up+down (моя независимая проверка) ⇒ систематически 50 хитов вместо 25 |
| **C5** стик-UP ≠ jump | Подтверждён без изменений | `GamepadInput.swift` |
| **V1** 0.5 vs 0.3, **V2** STEP 10 | Подтверждено + расширено | плюс ключи `UserDefaults` = `Exolon.Step10.*` (`GameState.swift:25-31`) — третий «след» версии |

## 4. Новые находки продукта (после TASK B архитектора; ★ — мои независимые подтверждения)

| ID | Pri | Находка | Где | Статус |
| --- | --- | --- | --- | --- |
| **B-01** | P1 | Спавн `vitorc` не согласован с коллизией в **90/125** карт: ровно под ногами пол только на 35. Остальные добираются глобальным `fallbackGroundY` (плоскость «поднимает» или «роняет» игрока на 16 px). ★ Моя независимая разбивка: **35 exact / 90 без локального пола** — сходится | `Player.swift:291-297`, `TMXLevelRuntime.swift:60-66` | код+данные |
| **B-02** | P2 | Глобальная невидимая плоскость `fallbackGroundY` удерживает игрока на выходе карты (в `L05S16` вся коллизия — один rect x=0..368, колонки 31..34 пусты — и всё равно не проваливаешься). Ямы безвредны всегда (`isGrounded` требует `footY > groundY+1.5`) | `Player.swift` | код+данные |
| **B-03** | P2 | **Ферма очков зоны 124:** бонус стадии применяется до проверки пустого `nextLevel`; цикл `.contentComplete → .title → beginFromTitle` не сбрасывает позицию/уровень ⇒ каждое «SPACE→SPACE» заново даёт `lives*1000`, refill и high score | `GameScene.swift:612,708-717,761-767` | код-трассировка; play-гипотеза |
| **B-04** | P2 | Хрупкость загрузчика: любой `compression` (zlib/gzip) → throw → `fatalError`; `makeMap()` не требует непустоты карты (well-formed XML с чужим корнем = играбельная пустота); опечатка в числовых атрибутах молча становится координатой 0. Сейчас ни одна карта не триггерит — риск на будущих данных. **Закрыто статикой** (ни один из 125 файлов не проходит эти ветки); отдельного play-пункта в §9 нет намеренно | `TMXMapLoader.swift:305-333`, `TMXLevelRuntime.swift:53` | код; ★ проверил: ни один из 125 TMX не сжат |
| **B-05** | P2 | Персистентность write-only: `GamePersistence.loadCheckpoint()` не вызывается нигде, при этом 7 ключей `Exolon.Step10.*` пишутся на смене зоны, а титул обещает «CONTINUE». Версионирования чекпоинта нет | `GameState.swift:24-31` | ★ grep: 0 вызовов |
| **B-06** | P2 | `windowDidResignKey` чистит только `.keyboard` — геймпад залипает; `pausePressPending` не сбрасывается; выхода из приложения нет кроме крестика окна (`Cmd+Q` не прокинут) | `GameView.swift:42-44` | код |
| **O8** (усилен) | P1 | `isActive` пусковой меняется **только** при сборе бонуса: ни бластер, ни граната её не убивают (нет в списках destructibles) ⇒ одна касательная бонуса навсегда гасит пушку (19 объектов на 18 картах) | `LevelObstacles.swift:699,718-723,431-478` | код+данные |
| **O7** (расширен) | P1 | См. §3: 10 карт × 2 поля × 25 хитов | | ★ |
| **D-01** | P1 | `LEVEL_COMPILER_AUDIT.md` — вход компилятора ASM, а не верификатор привезённых уровней; использовать его числа как «норму» нельзя | джойн docs_researcher | данные |
| **D-02** | P2 | 5 самых первых играемых зон (000-003, 006) без `imagelayer` — «original-visual pipeline» README неполон с первого экрана | TMX-скан | данные |
| **D-03** | P3 | `coordinateMode=tiledRect` ровно в 1/125 файлов, а README подаёт конвертацию rect как системное свойство; две разных геометрии exclusion для одного класса `capsule` (`max(96,w+64)` у маркера vs `w+32` у `source_marker`) | `TMXLevelRuntime.swift:304,377` | ★ код |

Не подтвердились (проверено с контролем и снято): туннелирование (шаг ≤5 px против 16-пиксельных клеток), «ловушка под потолком 52..63 px» (зазоры кратны 16), пересборка `SKScene` на уровень, ретейн-циклы, `try!`/`as!` — во всём дереве ноль.

## 5. Состояние factory-оверлея

- Состав: `factory/src/adaptive_factory` 25 413 LOC (Python, fastapi/psycopg; без `__pycache__`), тесты 13 368 LOC, policy-код `.grok-stack/adaptive_grok` 21 071 LOC (21 105 на весь `.grok-stack`); `factory/contracts` заполнен (28 jsonschema + 7 openapi), но загрузчиков из `src` почти не касается.
- Mandatory-тесты: **201 OK** (disposable-контейнер, PG 17.11), три прогона: FAIL-под-загрузкой(251 s, 1 ERROR)/OK(173 s)/OK(163 s, параллельно с агентами) → flaky с misleading-диагностикой (`StoreError: rejected` из анонимного SQL NULL в `semantic_bind_repair_child`) → upstream **#155**.
- Классификатор маршрутизации не знает Swift: репозиторий с 18 `.swift` + `.xcodeproj` получил `repo.languages: []`, `kind: generic`, `quality_profiles: [base]`; профиля Swift нет в `quality-profiles/` (ai/bitrix/contracts/data/frontend/infra/integration/php — без даже python) → upstream **#157**.
- `factory/README.md`: 7 из 8 относительных ссылок битые; в одном дереве версии v2.0.13/v2.0.14/`pyproject 0.1.0`/`product_version 2.0.18`.
- Дельта `403eb13..b8aee42` чистая: 24 файла, +1477/−4, продукт не тронут; добавлен один src-файл (`landing_http.py`, закрытие канала произвольного `error.code`) + 2 тест-модуля. `decisions.md` ↔ `factory-source.json`: sha256 `landing_http.py` ровно манифестный, `managed_file_count: 346` воспроизводится.

## 6. Гигиена и git-состояние репозитория

| Факт | Детали |
| --- | --- |
| Нет `.gitignore` | 8 untracked-записей `git status`: runtime-state `.grok-stack/runtime/*`, `__pycache__`, оба отчёта `engineering/reports/`, пакет аудита 7db1f3 — **доказательства нигде, кроме рабочего дерева** |
| Манифест-несоответствия | `AGENTS.md` ссылается на отсутствующие `START_HERE.md`, `PROJECT_STATE.json`, `mistakes.md`, `VERSION`, `architecture/*`, `trust-ci/`, `scripts/install_into.py` |
| Скелеты без содержимого | `engineering/adr`, `engineering/reviews`, `engineering/contracts/{openapi,asyncapi,schemas}` — пусто |
| Ветки/PR | `b8aee42` на remote под именем `codex/update-factory-20260920` → **открыт PR #1 (DRAFT)**; текущая локальная ветка `codex/factory-updated-20260920` — её неотправленный однофамилец; upstream-трекинга нет ни у одной локальной ветки; два внешних worktree в `~/.exolon-factory-update-20260920` |
| Схемы сборки | shared `.xcscheme` отсутствует — сборка только явным `-target Exolon` |
| Тесты продукта | XCTest у игры: 0 файлов |

## 7. Обновлённый приоритизированный бэклог (v2 — единственный актуальный)

**P1** — O8 (бонус гасит пусковую навсегда; 19 объектов на 18 картах) · O7 (50 хитов вместо 25, 10 карт) · B-01 (спавн vs пол, 90/125) · D-01 (документ-норма недостоверен) · O1 нижние пушки (18, walkthrough-зона 023) · O5 частичный бонус стадии + вращающийся «dormant»-комментарий.
**P2** — B-02 (невидимый глобальный пол/ямы) · B-03 (ферма очков 124) · B-04 (loader: zlib→fatalError, пустая карта без предупреждения) · B-05 (CONTINUE лжёт; чекпоинт write-only) · B-06 (залипание геймпада при потере фокуса, нет Cmd+Q) · C5 (стик-UP) · I1 (Options/pause, гипотеза) · F1 (title→SPACE) · D-02 (5 зон без imagelayer) · O2/O3/O4/O6/O9 (недоделки оригинала — без изменения статуса).
**P3** — V1 (0.5 vs 0.3) · V2 (три «номера шага»: окно Step 9, HUD STEP 9… и `Exolon.Step10.*` в UserDefaults) · D-03 (tiledRect 1/125; две геометрии exclusion кабин) · C1-demoted (4 кабины с 0-px запасом — добавить константу и юнит-гейт при первом же Swift-таргете) · отсутствие GIF-декодера (задекларировать статичность) · T2 (нет shared scheme) · T3 (нет XCTest) · hygiene-таблица §6.

## 8. Найдёнки окружения (вовне продукта)

- Подпись релиза Swift 6.4.0 (создана 14.09.2026) стоит **истёкшим** 16.09.2026 ключом «Swift 6.x Release Signing Key»; на keyserver обновлённой нет, в fresh all-keys.asc живой ключ один («Automatic Signing Key #4»). Целостность подтверждена (Good signature, подпись создана до истечения) — но автоматические пайплайны, форсирующие «не expired», на этом релизе упадут.
- `run_disposable_exit.py` не имеет `--help`-режима: неизвестный аргумент молча игнорируется и запускается полный прогон (сюрприз-футпринт; SIGPIPE-прерывание оставляет хвостовые venv в `/tmp`).

## 9. Обновлённый macOS-чеклист (заменяет §11 первичного аудита)

1. `xcodebuild -project Exolon.xcodeproj -target Exolon -configuration Debug build` + факт. версия бандла (0.3 vs 0.5).
2. Зона 009 (`L01S10`): кабина **работоспособна по формуле** — полный штатный флоу первичного §11.1: стоять на полу будки (не провалиться), один UP → EXOSKELETON ON без прыжка, **удержание → без повторных toggle, второй UP → OFF**, костюм ON: поршни не убивают / OFF: убивают (invuln выкл), D-pad/Cross работают, стик-UP — задокументировать отказ (C5), Pause→Restart: костюм снят, зона 000.
3. **Четыре маркерные кабины с запасом 0 px** (`L02S10`, `L03S11`, `L04S16`, `L05S10`, зоны 034/060/090/109): F1-хитбоксы — не вырезан ли пол exclusion-ом второй ветки (`w+32`, `:377`); ожидание: пол цел, кабина проходима; при провале — это реальный C1-механизм, но на этих картах, не на 009.
4. Зоны спавна из `evidence/fullaudit-b01-spawn.json` (90 без локального пола; взять крайние: 16-px подъём vs падение) — подтвердить, что `fallbackGroundY` ведёт себя как «плоскость».
5. Зона 006: бонус double launcher — **ожидается fail** (O8): после касания пушка мертва навсегда, ни одна пуля её не чинит.
6. Зоны 035 и 9 других с beam-парами (список в `evidence/fullaudit_measurements.py`): пересчитать хиты до снятия (ожидается 50 = 2×25).
7. Зона 124: FULL COMBAT ABILITY → title → SPACE → SPACE — **зафиксировать повторное начисление** `lives*1000` (B-03).
8. Титул: выбрать «CONTINUE» после смены зоны и перезапуска процесса — **ожидаемо ничего не происходит** (B-05).
9. Потеря фокуса окна при зажатом стике D-pad — проверить залипание (B-06); Cmd+Q — отсутствует.
10. Зоны 000–003, 006: фон без `imagelayer` — оценить визуально (D-02).
11. Остальное из первичного §11 (ввод, пауза, рестарт) — без изменений.

## 10. Доказательства

- Волна анализа (полные отчёты): `evidence/analysis-repo_explorer-fullaudit.md`, `evidence/analysis-architect-fullaudit.md`, `evidence/analysis-docs_researcher-fullaudit.md`
- **Воспроизводимые замеры продукта:** `python3 engineering/changes/20260919-exolon-initial-code-audit-7db1f3/evidence/fullaudit_measurements.py` — один скрипт: геометрия кабины L01S10 (C1) с must-fail контролем, beam-матрица 10/10, B-01-разбивка 35 exact/90 (персокартный список пишет в `evidence/fullaudit-b01-spawn.json`); rc=0 ⇔ все числа совпадают с отчётом
- Логи factory-контуров: `/tmp/exolon-factory-exit{,2,3}.log` (3 прогона: FAILED-1/OK/OK; воспроизводятся `python3 factory/tests/run_disposable_exit.py`)
- Прочие гейты (команды в §2/§3): xmllint 125/125; pngcheck 153 PNG; `swiftc -frontend -parse` 18/18; trailing-comma-аномалия L01S10
- Внешние артефакты: issues **Dimkox/adaptive-grok-build-pro#155** (flaky+диагностика bind_repair_child), **#157** (нет Swift-сигнала в классификаторе)
- Ревью-волна этого прогона: `evidence/code-review-fullaudit.md` (PASS, инкорпорирован), `evidence/test-review-fullaudit.md` (PASS с правками, инкорпорированы)
- Статусы верификации/ревью маршрута — в квитанциях `.grok-stack/runtime/receipts/7db1f3f0b126/` (перезаписываются на финальном дереве)

**Продуктовый код в этом маршруте не изменялся** (`git diff 403eb13 b8aee42 -- Exolon Exolon.xcodeproj` пуст; в рабочем дереве — только отчётно-доказательные файлы).
